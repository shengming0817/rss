schemaVersion = 1
id = "identity-refresh-localtx"
contractId = "identity.refresh"
txModel = "tenant-scoped-uow"
fixture = "fixtures/identity-refresh-localtx.toml"
runner = "journeys/tests/identity_refresh_localtx_journey.rs"
marker = "IDENTITY_REFRESH"

[[scenarios]]
kind = "happy"
applicable = true

[[scenarios]]
kind = "auth-failure"
applicable = true

[[scenarios]]
kind = "validation-failure"
applicable = true

[[scenarios]]
kind = "contention"
applicable = true

[[scenarios]]
kind = "conflict"
applicable = false
reason = "refresh reuse converges through atomic rotation and lineage revocation rather than exposing a business conflict"

[[scenarios]]
kind = "commit-unknown"
applicable = true

[package]
name = "rss-request-context"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license-file = "../../LICENSE"
readme = "README.md"
description = "Authority-free request context values and views for the RSS public Foundation"
categories = ["asynchronous", "data-structures"]
keywords = ["request", "context", "tenant", "authorization"]
publish = ["crates-io"]

[dependencies]
uuid.workspace = true

[features]
default = []

[lints]
workspace = true

[package]
name = "platform-execution-consumer"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
rss-contract = { path = "../../contract", default-features = false }
rss-platform = { path = "../../platform", default-features = false }
rss-request-context = { path = "../../request-context", default-features = false }
tokio = { version = "1", default-features = false, features = ["rt", "macros", "time"] }
serde_json = "1"

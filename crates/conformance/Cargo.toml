[package]
name = "rss-conformance"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = ["crates-io"]
license-file = "../../LICENSE"
repository.workspace = true
readme = "README.md"
description = "Provider-neutral conformance assertions for RSS engine primitives"
categories = ["development-tools::testing"]
keywords = ["conformance", "transaction", "testing", "provider", "localtx"]

[lints]
workspace = true

[dependencies]
thiserror = { workspace = true }

[features]
default = []

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }

[package]
name = "zeroize-probe"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
rss-data-protection = { path = "../.." }
rss-redact = { path = "../../../redact" }
rss-request-context = { path = "../../../request-context" }

uuid = "1"
time = "0.3"

[lints.rust]
unsafe_code = "deny"

[package]
name = "authmint"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[lints]
workspace = true

# Zero workspace / internal deps: capability token only. Consumers are gated by deny.toml wrappers
# (`httpserve`).

[package]
name = "ratelimit"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[lints]
workspace = true

# adapter 实现 diport DI port（RateLimiter + ManagedResource，native AFIT；不 invoke dynosaur 宏，保持 forbid）。
[dependencies]
# 实现 diport DI port trait（RateLimiter + ManagedResource）。
diport = { path = "../../crates/diport" }
rss-runtime.workspace = true
# keyed GCRA 限流器（x/time/rate token-bucket 的 Rust 工业等价），自带 monotonic 时钟。
# ref: antifuchs/governor src/state/keyed.rs
governor = { workspace = true }

[dev-dependencies]
# 异步单测 runtime（dyn-injection multi_thread spawn）。
tokio = { workspace = true }

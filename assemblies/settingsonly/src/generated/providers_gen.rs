// @generated by `cargo xtask assembly generate-providers`; DO NOT EDIT.
// Source: assemblies/settingsonly/assembly.toml
// Source-Manifest-Digest: sha256:d5a4b8de8f57e53cb47375f49a91d9afed1e6398b0f28ddc94f6ebe003876c76

use assembly_schema::{
    DiportPort, LifecycleChannel, ProviderCatalogEntry, ProviderConstructor, ProviderConsumer,
    ProviderDurability, ProviderFactorySymbol, ProviderRole,
};

pub(crate) const PROVIDER_CATALOG: &[ProviderCatalogEntry] = &[
    ProviderCatalogEntry::checked(
        ProviderRole::AuthAuditSink,
        DiportPort::AuditSink,
        ProviderConstructor::PostgresAuthAuditSink,
        ProviderFactorySymbol::HttpservePostgresAuthAuditSink,
        "postgres",
        &["auth-audit-sink"],
        ProviderConsumer::Httpserve,
        ProviderDurability::Persistent,
        None,
        None,
        &[
            LifecycleChannel::Probes,
            LifecycleChannel::Resources,
            LifecycleChannel::Workers,
        ],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::DistributedCasStore,
        DiportPort::Cas,
        ProviderConstructor::PostgresCasStore,
        ProviderFactorySymbol::DistributedPostgresCasStore,
        "postgres",
        &[],
        ProviderConsumer::Distributed,
        ProviderDurability::Persistent,
        None,
        None,
        &[
            LifecycleChannel::Probes,
            LifecycleChannel::Resources,
            LifecycleChannel::Workers,
        ],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::DistributedLockStore,
        DiportPort::Lock,
        ProviderConstructor::RedisLockStore,
        ProviderFactorySymbol::DistributedRedisLockStore,
        "redis",
        &["backend"],
        ProviderConsumer::Distributed,
        ProviderDurability::Persistent,
        None,
        None,
        &[
            LifecycleChannel::Probes,
            LifecycleChannel::Resources,
            LifecycleChannel::Workers,
        ],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::DlxArchiveKeyProvider,
        DiportPort::KeyProvider,
        ProviderConstructor::VaultKeyProvider,
        ProviderFactorySymbol::EventexecVaultArchiveKeyProvider,
        "vault",
        &["backend"],
        ProviderConsumer::Eventexec,
        ProviderDurability::Persistent,
        None,
        None,
        &[
            LifecycleChannel::Probes,
            LifecycleChannel::Resources,
            LifecycleChannel::Workers,
        ],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::DlxArchiveStore,
        DiportPort::DlxArchiveStore,
        ProviderConstructor::S3VerifiedDlxArchiveStore,
        ProviderFactorySymbol::EventexecS3DlxArchiveStore,
        "s3",
        &["backend"],
        ProviderConsumer::Eventexec,
        ProviderDurability::Persistent,
        None,
        None,
        &[LifecycleChannel::Probes, LifecycleChannel::Workers],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::DlxHotKeyProvider,
        DiportPort::KeyProvider,
        ProviderConstructor::VaultKeyProvider,
        ProviderFactorySymbol::EventexecVaultHotKeyProvider,
        "vault",
        &["backend"],
        ProviderConsumer::Eventexec,
        ProviderDurability::Persistent,
        None,
        None,
        &[
            LifecycleChannel::Probes,
            LifecycleChannel::Resources,
            LifecycleChannel::Workers,
        ],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::DlxLifecycleRepository,
        DiportPort::DlxLifecycleRepository,
        ProviderConstructor::PostgresDlxLifecycleRepository,
        ProviderFactorySymbol::EventexecPostgresDlxLifecycleRepository,
        "postgres",
        &[],
        ProviderConsumer::Eventexec,
        ProviderDurability::Persistent,
        None,
        None,
        &[
            LifecycleChannel::Probes,
            LifecycleChannel::Resources,
            LifecycleChannel::Workers,
        ],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::EventPublisher,
        DiportPort::Publisher,
        ProviderConstructor::AmqpPublisher,
        ProviderFactorySymbol::EventexecAmqpPublisher,
        "amqp",
        &["backend"],
        ProviderConsumer::Eventexec,
        ProviderDurability::Persistent,
        None,
        None,
        &[
            LifecycleChannel::Probes,
            LifecycleChannel::Resources,
            LifecycleChannel::Workers,
        ],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::EventSubscriber,
        DiportPort::AckableSubscriber,
        ProviderConstructor::AmqpSubscriber,
        ProviderFactorySymbol::EventexecAmqpSubscriber,
        "amqp",
        &["backend"],
        ProviderConsumer::Eventexec,
        ProviderDurability::Persistent,
        None,
        None,
        &[
            LifecycleChannel::Probes,
            LifecycleChannel::Resources,
            LifecycleChannel::Workers,
        ],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::ListenerPdp,
        DiportPort::Pdp,
        ProviderConstructor::OidcProvider,
        ProviderFactorySymbol::HttpserveOidcPdp,
        "oidc",
        &["backend"],
        ProviderConsumer::Httpserve,
        ProviderDurability::Persistent,
        None,
        None,
        &[LifecycleChannel::Resources],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::ListenerRateLimiter,
        DiportPort::RateLimiter,
        ProviderConstructor::RatelimitGovernorLimiter,
        ProviderFactorySymbol::HttpserveGovernorRateLimiter,
        "ratelimit",
        &[],
        ProviderConsumer::Httpserve,
        ProviderDurability::EphemeralMemory,
        None,
        None,
        &[],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::SettingsKeyProvider,
        DiportPort::KeyProvider,
        ProviderConstructor::VaultKeyProvider,
        ProviderFactorySymbol::SettingsVaultKeyProvider,
        "vault",
        &["backend"],
        ProviderConsumer::Settings,
        ProviderDurability::Persistent,
        None,
        None,
        &[
            LifecycleChannel::Probes,
            LifecycleChannel::Resources,
            LifecycleChannel::Workers,
        ],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::SettingsSecretResolver,
        DiportPort::SecretResolver,
        ProviderConstructor::VaultSecretResolver,
        ProviderFactorySymbol::SettingsVaultSecretResolver,
        "vault",
        &["backend"],
        ProviderConsumer::Settings,
        ProviderDurability::Persistent,
        None,
        None,
        &[
            LifecycleChannel::Probes,
            LifecycleChannel::Resources,
            LifecycleChannel::Workers,
        ],
    ),
];

pub(crate) struct ProviderRoleBatches {
    auth_audit_sink: Option<AuthAuditSinkConstructor>,
    distributed_cas_store: Option<DistributedCasStoreConstructor>,
    distributed_lock_store: Option<DistributedLockStoreConstructor>,
    dlx_archive_key_provider: Option<DlxArchiveKeyProviderConstructor>,
    dlx_archive_store: Option<DlxArchiveStoreConstructor>,
    dlx_hot_key_provider: Option<DlxHotKeyProviderConstructor>,
    dlx_lifecycle_repository: Option<DlxLifecycleRepositoryConstructor>,
    event_publisher: Option<EventPublisherConstructor>,
    event_subscriber: Option<EventSubscriberConstructor>,
    listener_pdp: Option<ListenerPdpConstructor>,
    listener_rate_limiter: Option<ListenerRateLimiterConstructor>,
    settings_key_provider: Option<SettingsKeyProviderConstructor>,
    settings_secret_resolver: Option<SettingsSecretResolverConstructor>,
}

pub(crate) struct CompletedProviderRoles {
    probe_bindings: Vec<runtimeexec::inventory::ProviderProbeBinding>,
}

impl CompletedProviderRoles {
    pub(crate) fn into_probe_bindings(self) -> Vec<runtimeexec::inventory::ProviderProbeBinding> {
        self.probe_bindings
    }
}

impl ProviderRoleBatches {
    pub(crate) fn exact_join(plans: &[assembly_schema::ProviderPlan]) -> anyhow::Result<Self> {
        anyhow::ensure!(
            plans.len() == PROVIDER_CATALOG.len(),
            "settingsonly RuntimePlan/generated provider catalog count drift"
        );
        let mut batches = Self {
            auth_audit_sink: None,
            distributed_cas_store: None,
            distributed_lock_store: None,
            dlx_archive_key_provider: None,
            dlx_archive_store: None,
            dlx_hot_key_provider: None,
            dlx_lifecycle_repository: None,
            event_publisher: None,
            event_subscriber: None,
            listener_pdp: None,
            listener_rate_limiter: None,
            settings_key_provider: None,
            settings_secret_resolver: None,
        };
        for entry in PROVIDER_CATALOG {
            let mut matching = plans
                .iter()
                .filter(|plan| plan.id() == entry.role().as_str());
            let plan = matching.next().ok_or_else(|| {
                anyhow::anyhow!(
                    "settingsonly RuntimePlan omits generated provider role '{}'",
                    entry.role().as_str()
                )
            })?;
            anyhow::ensure!(
                matching.next().is_none(),
                "settingsonly RuntimePlan duplicates generated provider role '{}'",
                entry.role().as_str()
            );
            anyhow::ensure!(
                plan.constructor() == entry.evidence().constructor()
                    && plan.outputs() == entry.evidence().outputs(),
                "settingsonly RuntimePlan disagrees with generated provider role '{}'",
                entry.role().as_str()
            );
            match entry.role() {
                ProviderRole::AuthAuditSink => anyhow::ensure!(
                    batches
                        .auth_audit_sink
                        .replace(AuthAuditSinkConstructor { entry })
                        .is_none(),
                    "settingsonly generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::DistributedCasStore => anyhow::ensure!(
                    batches
                        .distributed_cas_store
                        .replace(DistributedCasStoreConstructor { entry })
                        .is_none(),
                    "settingsonly generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::DistributedLockStore => anyhow::ensure!(
                    batches
                        .distributed_lock_store
                        .replace(DistributedLockStoreConstructor { entry })
                        .is_none(),
                    "settingsonly generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::DlxArchiveKeyProvider => anyhow::ensure!(
                    batches
                        .dlx_archive_key_provider
                        .replace(DlxArchiveKeyProviderConstructor { entry })
                        .is_none(),
                    "settingsonly generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::DlxArchiveStore => anyhow::ensure!(
                    batches
                        .dlx_archive_store
                        .replace(DlxArchiveStoreConstructor { entry })
                        .is_none(),
                    "settingsonly generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::DlxHotKeyProvider => anyhow::ensure!(
                    batches
                        .dlx_hot_key_provider
                        .replace(DlxHotKeyProviderConstructor { entry })
                        .is_none(),
                    "settingsonly generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::DlxLifecycleRepository => anyhow::ensure!(
                    batches
                        .dlx_lifecycle_repository
                        .replace(DlxLifecycleRepositoryConstructor { entry })
                        .is_none(),
                    "settingsonly generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::EventPublisher => anyhow::ensure!(
                    batches
                        .event_publisher
                        .replace(EventPublisherConstructor { entry })
                        .is_none(),
                    "settingsonly generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::EventSubscriber => anyhow::ensure!(
                    batches
                        .event_subscriber
                        .replace(EventSubscriberConstructor { entry })
                        .is_none(),
                    "settingsonly generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::ListenerPdp => anyhow::ensure!(
                    batches
                        .listener_pdp
                        .replace(ListenerPdpConstructor { entry })
                        .is_none(),
                    "settingsonly generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::ListenerRateLimiter => anyhow::ensure!(
                    batches
                        .listener_rate_limiter
                        .replace(ListenerRateLimiterConstructor { entry })
                        .is_none(),
                    "settingsonly generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::SettingsKeyProvider => anyhow::ensure!(
                    batches
                        .settings_key_provider
                        .replace(SettingsKeyProviderConstructor { entry })
                        .is_none(),
                    "settingsonly generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::SettingsSecretResolver => anyhow::ensure!(
                    batches
                        .settings_secret_resolver
                        .replace(SettingsSecretResolverConstructor { entry })
                        .is_none(),
                    "settingsonly generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                _ => anyhow::bail!(
                    "settingsonly generated provider catalog contains unsupported role '{}'",
                    entry.role().as_str()
                ),
            }
        }
        batches.require_complete()?;
        Ok(batches)
    }
    fn require_complete(&self) -> anyhow::Result<()> {
        anyhow::ensure!(
            self.auth_audit_sink.is_some(),
            "settingsonly generated provider role 'auth-audit-sink' is missing"
        );
        anyhow::ensure!(
            self.distributed_cas_store.is_some(),
            "settingsonly generated provider role 'distributed-cas-store' is missing"
        );
        anyhow::ensure!(
            self.distributed_lock_store.is_some(),
            "settingsonly generated provider role 'distributed-lock-store' is missing"
        );
        anyhow::ensure!(
            self.dlx_archive_key_provider.is_some(),
            "settingsonly generated provider role 'dlx-archive-key-provider' is missing"
        );
        anyhow::ensure!(
            self.dlx_archive_store.is_some(),
            "settingsonly generated provider role 'dlx-archive-store' is missing"
        );
        anyhow::ensure!(
            self.dlx_hot_key_provider.is_some(),
            "settingsonly generated provider role 'dlx-hot-key-provider' is missing"
        );
        anyhow::ensure!(
            self.dlx_lifecycle_repository.is_some(),
            "settingsonly generated provider role 'dlx-lifecycle-repository' is missing"
        );
        anyhow::ensure!(
            self.event_publisher.is_some(),
            "settingsonly generated provider role 'event-publisher' is missing"
        );
        anyhow::ensure!(
            self.event_subscriber.is_some(),
            "settingsonly generated provider role 'event-subscriber' is missing"
        );
        anyhow::ensure!(
            self.listener_pdp.is_some(),
            "settingsonly generated provider role 'listener-pdp' is missing"
        );
        anyhow::ensure!(
            self.listener_rate_limiter.is_some(),
            "settingsonly generated provider role 'listener-rate-limiter' is missing"
        );
        anyhow::ensure!(
            self.settings_key_provider.is_some(),
            "settingsonly generated provider role 'settings-key-provider' is missing"
        );
        anyhow::ensure!(
            self.settings_secret_resolver.is_some(),
            "settingsonly generated provider role 'settings-secret-resolver' is missing"
        );
        Ok(())
    }
    pub(crate) fn auth_audit_sink(&mut self) -> anyhow::Result<AuthAuditSinkConstructor> {
        self.auth_audit_sink.take().ok_or_else(|| {
            anyhow::anyhow!(
                "settingsonly provider constructor 'auth-audit-sink' was consumed more than once"
            )
        })
    }
    pub(crate) fn distributed_cas_store(
        &mut self,
    ) -> anyhow::Result<DistributedCasStoreConstructor> {
        self.distributed_cas_store.take().ok_or_else(|| anyhow::anyhow!("settingsonly provider constructor 'distributed-cas-store' was consumed more than once"))
    }
    pub(crate) fn distributed_lock_store(
        &mut self,
    ) -> anyhow::Result<DistributedLockStoreConstructor> {
        self.distributed_lock_store.take().ok_or_else(|| anyhow::anyhow!("settingsonly provider constructor 'distributed-lock-store' was consumed more than once"))
    }
    pub(crate) fn dlx_archive_key_provider(
        &mut self,
    ) -> anyhow::Result<DlxArchiveKeyProviderConstructor> {
        self.dlx_archive_key_provider.take().ok_or_else(|| anyhow::anyhow!("settingsonly provider constructor 'dlx-archive-key-provider' was consumed more than once"))
    }
    pub(crate) fn dlx_archive_store(&mut self) -> anyhow::Result<DlxArchiveStoreConstructor> {
        self.dlx_archive_store.take().ok_or_else(|| {
            anyhow::anyhow!(
                "settingsonly provider constructor 'dlx-archive-store' was consumed more than once"
            )
        })
    }
    pub(crate) fn dlx_hot_key_provider(&mut self) -> anyhow::Result<DlxHotKeyProviderConstructor> {
        self.dlx_hot_key_provider.take().ok_or_else(|| anyhow::anyhow!("settingsonly provider constructor 'dlx-hot-key-provider' was consumed more than once"))
    }
    pub(crate) fn dlx_lifecycle_repository(
        &mut self,
    ) -> anyhow::Result<DlxLifecycleRepositoryConstructor> {
        self.dlx_lifecycle_repository.take().ok_or_else(|| anyhow::anyhow!("settingsonly provider constructor 'dlx-lifecycle-repository' was consumed more than once"))
    }
    pub(crate) fn event_publisher(&mut self) -> anyhow::Result<EventPublisherConstructor> {
        self.event_publisher.take().ok_or_else(|| {
            anyhow::anyhow!(
                "settingsonly provider constructor 'event-publisher' was consumed more than once"
            )
        })
    }
    pub(crate) fn event_subscriber(&mut self) -> anyhow::Result<EventSubscriberConstructor> {
        self.event_subscriber.take().ok_or_else(|| {
            anyhow::anyhow!(
                "settingsonly provider constructor 'event-subscriber' was consumed more than once"
            )
        })
    }
    pub(crate) fn listener_pdp(&mut self) -> anyhow::Result<ListenerPdpConstructor> {
        self.listener_pdp.take().ok_or_else(|| {
            anyhow::anyhow!(
                "settingsonly provider constructor 'listener-pdp' was consumed more than once"
            )
        })
    }
    pub(crate) fn listener_rate_limiter(
        &mut self,
    ) -> anyhow::Result<ListenerRateLimiterConstructor> {
        self.listener_rate_limiter.take().ok_or_else(|| anyhow::anyhow!("settingsonly provider constructor 'listener-rate-limiter' was consumed more than once"))
    }
    pub(crate) fn settings_key_provider(
        &mut self,
    ) -> anyhow::Result<SettingsKeyProviderConstructor> {
        self.settings_key_provider.take().ok_or_else(|| anyhow::anyhow!("settingsonly provider constructor 'settings-key-provider' was consumed more than once"))
    }
    pub(crate) fn settings_secret_resolver(
        &mut self,
    ) -> anyhow::Result<SettingsSecretResolverConstructor> {
        self.settings_secret_resolver.take().ok_or_else(|| anyhow::anyhow!("settingsonly provider constructor 'settings-secret-resolver' was consumed more than once"))
    }
    #[allow(clippy::too_many_arguments)]
    // reason: generated exact-join closure has one move-only receipt per declared provider role.
    pub(crate) fn finish(
        self,
        inventory: &bootstrap::DomainModuleResult,
        auth_audit_sink: AuthAuditSinkReceipt,
        distributed_cas_store: DistributedCasStoreReceipt,
        distributed_lock_store: DistributedLockStoreReceipt,
        dlx_archive_key_provider: DlxArchiveKeyProviderReceipt,
        dlx_archive_store: DlxArchiveStoreReceipt,
        dlx_hot_key_provider: DlxHotKeyProviderReceipt,
        dlx_lifecycle_repository: DlxLifecycleRepositoryReceipt,
        event_publisher: EventPublisherReceipt,
        event_subscriber: EventSubscriberReceipt,
        listener_pdp: ListenerPdpReceipt,
        listener_rate_limiter: ListenerRateLimiterReceipt,
        settings_key_provider: SettingsKeyProviderReceipt,
        settings_secret_resolver: SettingsSecretResolverReceipt,
    ) -> anyhow::Result<CompletedProviderRoles> {
        let mut staged = [0_usize; 3];
        let mut probe_bindings = Vec::with_capacity(PROVIDER_CATALOG.len());
        let AuthAuditSinkReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = auth_audit_sink;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(runtimeexec::inventory::ProviderProbeBinding::new(
            "auth-audit-sink",
            probe_names,
        )?);
        let DistributedCasStoreReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = distributed_cas_store;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(runtimeexec::inventory::ProviderProbeBinding::new(
            "distributed-cas-store",
            probe_names,
        )?);
        let DistributedLockStoreReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = distributed_lock_store;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(runtimeexec::inventory::ProviderProbeBinding::new(
            "distributed-lock-store",
            probe_names,
        )?);
        let DlxArchiveKeyProviderReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = dlx_archive_key_provider;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(runtimeexec::inventory::ProviderProbeBinding::new(
            "dlx-archive-key-provider",
            probe_names,
        )?);
        let DlxArchiveStoreReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = dlx_archive_store;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(runtimeexec::inventory::ProviderProbeBinding::new(
            "dlx-archive-store",
            probe_names,
        )?);
        let DlxHotKeyProviderReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = dlx_hot_key_provider;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(runtimeexec::inventory::ProviderProbeBinding::new(
            "dlx-hot-key-provider",
            probe_names,
        )?);
        let DlxLifecycleRepositoryReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = dlx_lifecycle_repository;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(runtimeexec::inventory::ProviderProbeBinding::new(
            "dlx-lifecycle-repository",
            probe_names,
        )?);
        let EventPublisherReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = event_publisher;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(runtimeexec::inventory::ProviderProbeBinding::new(
            "event-publisher",
            probe_names,
        )?);
        let EventSubscriberReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = event_subscriber;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(runtimeexec::inventory::ProviderProbeBinding::new(
            "event-subscriber",
            probe_names,
        )?);
        let ListenerPdpReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = listener_pdp;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(runtimeexec::inventory::ProviderProbeBinding::new(
            "listener-pdp",
            probe_names,
        )?);
        let ListenerRateLimiterReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = listener_rate_limiter;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(runtimeexec::inventory::ProviderProbeBinding::new(
            "listener-rate-limiter",
            probe_names,
        )?);
        let SettingsKeyProviderReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = settings_key_provider;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(runtimeexec::inventory::ProviderProbeBinding::new(
            "settings-key-provider",
            probe_names,
        )?);
        let SettingsSecretResolverReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = settings_secret_resolver;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(runtimeexec::inventory::ProviderProbeBinding::new(
            "settings-secret-resolver",
            probe_names,
        )?);
        if self.auth_audit_sink.is_some()
            || self.distributed_cas_store.is_some()
            || self.distributed_lock_store.is_some()
            || self.dlx_archive_key_provider.is_some()
            || self.dlx_archive_store.is_some()
            || self.dlx_hot_key_provider.is_some()
            || self.dlx_lifecycle_repository.is_some()
            || self.event_publisher.is_some()
            || self.event_subscriber.is_some()
            || self.listener_pdp.is_some()
            || self.listener_rate_limiter.is_some()
            || self.settings_key_provider.is_some()
            || self.settings_secret_resolver.is_some()
        {
            anyhow::bail!(
                "settingsonly provider role receipt came from a different exact-join generation"
            );
        }
        anyhow::ensure!(
            inventory.probes.len() == staged[0]
                && inventory.resources.len() == staged[1]
                && inventory.workers.len() == staged[2],
            "settingsonly transaction provider lifecycle output differs from exact receipts"
        );
        Ok(CompletedProviderRoles { probe_bindings })
    }
}

fn lifecycle_channels(output: &bootstrap::DomainModuleResult) -> Vec<LifecycleChannel> {
    let mut channels = Vec::new();
    if !output.probes.is_empty() {
        channels.push(LifecycleChannel::Probes);
    }
    if !output.resources.is_empty() {
        channels.push(LifecycleChannel::Resources);
    }
    if !output.workers.is_empty() {
        channels.push(LifecycleChannel::Workers);
    }
    channels
}

fn validate_lifecycle_output(
    entry: &ProviderCatalogEntry,
    output: &bootstrap::DomainModuleResult,
) -> anyhow::Result<()> {
    let actual = lifecycle_channels(output);
    anyhow::ensure!(
        actual == entry.evidence().outputs(),
        "settingsonly provider role '{}' lifecycle mismatch: expected {:?}, actual {:?}",
        entry.role().as_str(),
        entry.evidence().outputs(),
        actual
    );
    Ok(())
}

pub(crate) struct AuthAuditSinkConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct AuthAuditSinkBatch(bootstrap::DomainModuleResult);
pub(crate) struct AuthAuditSinkReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl AuthAuditSinkConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<AuthAuditSinkBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(AuthAuditSinkBatch(output))
    }
}
impl AuthAuditSinkBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> AuthAuditSinkReceipt {
        let probe_names = self.0.probes.iter().map(|(name, _)| name.clone()).collect();
        let receipt = AuthAuditSinkReceipt {
            probes: self.0.probes.len(),
            resources: self.0.resources.len(),
            workers: self.0.workers.len(),
            probe_names,
        };
        inventory.merge(self.0);
        receipt
    }
}

pub(crate) struct DistributedCasStoreConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct DistributedCasStoreBatch(bootstrap::DomainModuleResult);
pub(crate) struct DistributedCasStoreReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl DistributedCasStoreConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<DistributedCasStoreBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(DistributedCasStoreBatch(output))
    }
}
impl DistributedCasStoreBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> DistributedCasStoreReceipt {
        let probe_names = self.0.probes.iter().map(|(name, _)| name.clone()).collect();
        let receipt = DistributedCasStoreReceipt {
            probes: self.0.probes.len(),
            resources: self.0.resources.len(),
            workers: self.0.workers.len(),
            probe_names,
        };
        inventory.merge(self.0);
        receipt
    }
}

pub(crate) struct DistributedLockStoreConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct DistributedLockStoreBatch(bootstrap::DomainModuleResult);
pub(crate) struct DistributedLockStoreReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl DistributedLockStoreConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<DistributedLockStoreBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(DistributedLockStoreBatch(output))
    }
}
impl DistributedLockStoreBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> DistributedLockStoreReceipt {
        let probe_names = self.0.probes.iter().map(|(name, _)| name.clone()).collect();
        let receipt = DistributedLockStoreReceipt {
            probes: self.0.probes.len(),
            resources: self.0.resources.len(),
            workers: self.0.workers.len(),
            probe_names,
        };
        inventory.merge(self.0);
        receipt
    }
}

pub(crate) struct DlxArchiveKeyProviderConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct DlxArchiveKeyProviderBatch(bootstrap::DomainModuleResult);
pub(crate) struct DlxArchiveKeyProviderReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl DlxArchiveKeyProviderConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<DlxArchiveKeyProviderBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(DlxArchiveKeyProviderBatch(output))
    }
}
impl DlxArchiveKeyProviderBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> DlxArchiveKeyProviderReceipt {
        let probe_names = self.0.probes.iter().map(|(name, _)| name.clone()).collect();
        let receipt = DlxArchiveKeyProviderReceipt {
            probes: self.0.probes.len(),
            resources: self.0.resources.len(),
            workers: self.0.workers.len(),
            probe_names,
        };
        inventory.merge(self.0);
        receipt
    }
}

pub(crate) struct DlxArchiveStoreConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct DlxArchiveStoreBatch(bootstrap::DomainModuleResult);
pub(crate) struct DlxArchiveStoreReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl DlxArchiveStoreConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<DlxArchiveStoreBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(DlxArchiveStoreBatch(output))
    }
}
impl DlxArchiveStoreBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> DlxArchiveStoreReceipt {
        let probe_names = self.0.probes.iter().map(|(name, _)| name.clone()).collect();
        let receipt = DlxArchiveStoreReceipt {
            probes: self.0.probes.len(),
            resources: self.0.resources.len(),
            workers: self.0.workers.len(),
            probe_names,
        };
        inventory.merge(self.0);
        receipt
    }
}

pub(crate) struct DlxHotKeyProviderConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct DlxHotKeyProviderBatch(bootstrap::DomainModuleResult);
pub(crate) struct DlxHotKeyProviderReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl DlxHotKeyProviderConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<DlxHotKeyProviderBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(DlxHotKeyProviderBatch(output))
    }
}
impl DlxHotKeyProviderBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> DlxHotKeyProviderReceipt {
        let probe_names = self.0.probes.iter().map(|(name, _)| name.clone()).collect();
        let receipt = DlxHotKeyProviderReceipt {
            probes: self.0.probes.len(),
            resources: self.0.resources.len(),
            workers: self.0.workers.len(),
            probe_names,
        };
        inventory.merge(self.0);
        receipt
    }
}

pub(crate) struct DlxLifecycleRepositoryConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct DlxLifecycleRepositoryBatch(bootstrap::DomainModuleResult);
pub(crate) struct DlxLifecycleRepositoryReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl DlxLifecycleRepositoryConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<DlxLifecycleRepositoryBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(DlxLifecycleRepositoryBatch(output))
    }
}
impl DlxLifecycleRepositoryBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> DlxLifecycleRepositoryReceipt {
        let probe_names = self.0.probes.iter().map(|(name, _)| name.clone()).collect();
        let receipt = DlxLifecycleRepositoryReceipt {
            probes: self.0.probes.len(),
            resources: self.0.resources.len(),
            workers: self.0.workers.len(),
            probe_names,
        };
        inventory.merge(self.0);
        receipt
    }
}

pub(crate) struct EventPublisherConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct EventPublisherBatch(bootstrap::DomainModuleResult);
pub(crate) struct EventPublisherReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl EventPublisherConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<EventPublisherBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(EventPublisherBatch(output))
    }
}
impl EventPublisherBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> EventPublisherReceipt {
        let probe_names = self.0.probes.iter().map(|(name, _)| name.clone()).collect();
        let receipt = EventPublisherReceipt {
            probes: self.0.probes.len(),
            resources: self.0.resources.len(),
            workers: self.0.workers.len(),
            probe_names,
        };
        inventory.merge(self.0);
        receipt
    }
}

pub(crate) struct EventSubscriberConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct EventSubscriberBatch(bootstrap::DomainModuleResult);
pub(crate) struct EventSubscriberReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl EventSubscriberConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<EventSubscriberBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(EventSubscriberBatch(output))
    }
}
impl EventSubscriberBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> EventSubscriberReceipt {
        let probe_names = self.0.probes.iter().map(|(name, _)| name.clone()).collect();
        let receipt = EventSubscriberReceipt {
            probes: self.0.probes.len(),
            resources: self.0.resources.len(),
            workers: self.0.workers.len(),
            probe_names,
        };
        inventory.merge(self.0);
        receipt
    }
}

pub(crate) struct ListenerPdpConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct ListenerPdpBatch(bootstrap::DomainModuleResult);
pub(crate) struct ListenerPdpReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl ListenerPdpConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<ListenerPdpBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(ListenerPdpBatch(output))
    }
}
impl ListenerPdpBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> ListenerPdpReceipt {
        let probe_names = self.0.probes.iter().map(|(name, _)| name.clone()).collect();
        let receipt = ListenerPdpReceipt {
            probes: self.0.probes.len(),
            resources: self.0.resources.len(),
            workers: self.0.workers.len(),
            probe_names,
        };
        inventory.merge(self.0);
        receipt
    }
}

pub(crate) struct ListenerRateLimiterConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct ListenerRateLimiterBatch(bootstrap::DomainModuleResult);
pub(crate) struct ListenerRateLimiterReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl ListenerRateLimiterConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<ListenerRateLimiterBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(ListenerRateLimiterBatch(output))
    }
}
impl ListenerRateLimiterBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> ListenerRateLimiterReceipt {
        let probe_names = self.0.probes.iter().map(|(name, _)| name.clone()).collect();
        let receipt = ListenerRateLimiterReceipt {
            probes: self.0.probes.len(),
            resources: self.0.resources.len(),
            workers: self.0.workers.len(),
            probe_names,
        };
        inventory.merge(self.0);
        receipt
    }
}

pub(crate) struct SettingsKeyProviderConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct SettingsKeyProviderBatch(bootstrap::DomainModuleResult);
pub(crate) struct SettingsKeyProviderReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl SettingsKeyProviderConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<SettingsKeyProviderBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(SettingsKeyProviderBatch(output))
    }
}
impl SettingsKeyProviderBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> SettingsKeyProviderReceipt {
        let probe_names = self.0.probes.iter().map(|(name, _)| name.clone()).collect();
        let receipt = SettingsKeyProviderReceipt {
            probes: self.0.probes.len(),
            resources: self.0.resources.len(),
            workers: self.0.workers.len(),
            probe_names,
        };
        inventory.merge(self.0);
        receipt
    }
}

pub(crate) struct SettingsSecretResolverConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct SettingsSecretResolverBatch(bootstrap::DomainModuleResult);
pub(crate) struct SettingsSecretResolverReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl SettingsSecretResolverConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<SettingsSecretResolverBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(SettingsSecretResolverBatch(output))
    }
}
impl SettingsSecretResolverBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> SettingsSecretResolverReceipt {
        let probe_names = self.0.probes.iter().map(|(name, _)| name.clone()).collect();
        let receipt = SettingsSecretResolverReceipt {
            probes: self.0.probes.len(),
            resources: self.0.resources.len(),
            workers: self.0.workers.len(),
            probe_names,
        };
        inventory.merge(self.0);
        receipt
    }
}

// @generated by `cargo xtask assembly generate-providers`; DO NOT EDIT.
// Source: assemblies/deviceidentity/assembly.toml
// Source-Manifest-Digest: sha256:e007fbd86bd681286800fc297b9843f957cb8512d048c56cc08b5211010e9049

use assembly_schema::{
    AssemblyDomain, DiportPort, LifecycleChannel, ProviderActivation, ProviderCatalogEntry,
    ProviderConstructor, ProviderConsumer, ProviderDurability, ProviderFactorySymbol, ProviderRole,
};

pub(crate) const ASSEMBLY_NAMESPACE: &str = "deviceidentity";

pub(crate) const PROVIDER_CATALOG: &[ProviderCatalogEntry] = &[
    ProviderCatalogEntry::checked(
        ProviderRole::AuthAuditSink,
        ProviderActivation::Process,
        DiportPort::AuditSink,
        ProviderConstructor::PostgresAuthAuditSink,
        ProviderFactorySymbol::HttpservePostgresAuthAuditSink,
        "postgres",
        &["auth-audit-sink"],
        ProviderConsumer::Httpserve,
        ProviderDurability::Persistent,
        None,
        None,
        &[
            LifecycleChannel::Probes,
            LifecycleChannel::Resources,
            LifecycleChannel::Workers,
        ],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::DeviceCertificateStore,
        ProviderActivation::DomainLocal(AssemblyDomain::Identity),
        DiportPort::CertificateReconcileRepository,
        ProviderConstructor::PostgresDeviceCertificateRepository,
        ProviderFactorySymbol::IdentityPostgresDeviceCertificateStore,
        "postgres",
        &["domain-identity"],
        ProviderConsumer::Identity,
        ProviderDurability::Persistent,
        None,
        None,
        &[LifecycleChannel::Probes],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::DeviceCommandStore,
        ProviderActivation::DomainLocal(AssemblyDomain::Identity),
        DiportPort::DeviceCommandStore,
        ProviderConstructor::PostgresDeviceCommandStore,
        ProviderFactorySymbol::IdentityPostgresDeviceCommandStore,
        "postgres",
        &["domain-identity"],
        ProviderConsumer::Identity,
        ProviderDurability::Persistent,
        None,
        None,
        &[LifecycleChannel::Probes, LifecycleChannel::Workers],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::DeviceMqttSession,
        ProviderActivation::DomainLocal(AssemblyDomain::Identity),
        DiportPort::MqttSession,
        ProviderConstructor::MqttSession,
        ProviderFactorySymbol::IdentityMqttSession,
        "mqtt",
        &[],
        ProviderConsumer::Identity,
        ProviderDurability::Persistent,
        None,
        None,
        &[
            LifecycleChannel::Probes,
            LifecycleChannel::Resources,
            LifecycleChannel::Workers,
        ],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::DeviceProductionArtifactSource,
        ProviderActivation::DomainLocal(AssemblyDomain::Identity),
        DiportPort::CertificateArtifactSource,
        ProviderConstructor::IdentityExternalPkiArtifactSource,
        ProviderFactorySymbol::IdentityExternalPkiArtifactSource,
        "identity-composition",
        &[],
        ProviderConsumer::Identity,
        ProviderDurability::Persistent,
        None,
        None,
        &[],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::DeviceRevocationStore,
        ProviderActivation::Process,
        DiportPort::RevocationStore,
        ProviderConstructor::PostgresRevocationStore,
        ProviderFactorySymbol::DeviceloopPostgresRevocationStore,
        "postgres",
        &[],
        ProviderConsumer::Deviceloop,
        ProviderDurability::Persistent,
        None,
        None,
        &[LifecycleChannel::Probes, LifecycleChannel::Workers],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::ExternalCsrResolver,
        ProviderActivation::DomainLocal(AssemblyDomain::Identity),
        DiportPort::ExternalCsrResolver,
        ProviderConstructor::HttpdSpiffeMtlsExternalCsrResolver,
        ProviderFactorySymbol::IdentityExternalCsrResolver,
        "httpd",
        &[],
        ProviderConsumer::Identity,
        ProviderDurability::Persistent,
        None,
        None,
        &[LifecycleChannel::Probes, LifecycleChannel::Resources],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::ListenerPdp,
        ProviderActivation::Process,
        DiportPort::Pdp,
        ProviderConstructor::OidcProvider,
        ProviderFactorySymbol::HttpserveOidcPdp,
        "oidc",
        &["backend"],
        ProviderConsumer::Httpserve,
        ProviderDurability::Persistent,
        None,
        None,
        &[LifecycleChannel::Probes, LifecycleChannel::Resources],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::ListenerRateLimiter,
        ProviderActivation::Process,
        DiportPort::RateLimiter,
        ProviderConstructor::RedisRateLimiter,
        ProviderFactorySymbol::HttpserveRedisRateLimiter,
        "redis",
        &["backend"],
        ProviderConsumer::Httpserve,
        ProviderDurability::Persistent,
        Some(assembly_schema::ProviderScope::ClusterGlobal),
        Some(assembly_schema::ProviderFailurePosture::FailOpen),
        &[],
    ),
    ProviderCatalogEntry::checked(
        ProviderRole::VaultExternalPki,
        ProviderActivation::DomainLocal(AssemblyDomain::Identity),
        DiportPort::ExternalPkiProviderClosure,
        ProviderConstructor::VaultExternalPkiProviderClosure,
        ProviderFactorySymbol::IdentityVaultExternalPki,
        "vault",
        &["backend"],
        ProviderConsumer::Identity,
        ProviderDurability::Persistent,
        None,
        None,
        &[LifecycleChannel::Probes, LifecycleChannel::Resources],
    ),
];

struct ListenerPdpJwksEntry {
    probe: (primitives::ProbeName, Box<dyn bootstrap::HealthProbe>),
    resource: Box<diport::DynManagedResource<'static>>,
}

#[must_use = "listener PDP JWKS lifecycle must be committed through its typed provider transaction"]
pub(crate) struct ListenerPdpJwksLifecycle {
    head: ListenerPdpJwksEntry,
    tail: Vec<ListenerPdpJwksEntry>,
}

impl ListenerPdpJwksLifecycle {
    pub(crate) fn single(
        probe: (primitives::ProbeName, Box<dyn bootstrap::HealthProbe>),
        resource: Box<diport::DynManagedResource<'static>>,
    ) -> Self {
        Self {
            head: ListenerPdpJwksEntry { probe, resource },
            tail: Vec::new(),
        }
    }

    #[allow(
        dead_code,
        reason = "single-entry assemblies share the canonical carrier API"
    )]
    pub(crate) fn merge(mut self, other: Self) -> Self {
        self.tail.push(other.head);
        self.tail.extend(other.tail);
        self
    }

    pub(crate) fn into_output(self) -> bootstrap::DomainModuleResult {
        let (probes, resources): (Vec<_>, Vec<_>) = std::iter::once(self.head)
            .chain(self.tail)
            .map(|entry| (entry.probe, entry.resource))
            .unzip();
        bootstrap::DomainModuleResult::from_parts(probes, resources, [])
    }
}

pub(crate) struct ProviderRoleBatches {
    auth_audit_sink: Option<AuthAuditSinkConstructor>,
    device_certificate_store: Option<DeviceCertificateStoreConstructor>,
    device_command_store: Option<DeviceCommandStoreConstructor>,
    device_mqtt_session: Option<DeviceMqttSessionConstructor>,
    device_production_artifact_source: Option<DeviceProductionArtifactSourceConstructor>,
    device_revocation_store: Option<DeviceRevocationStoreConstructor>,
    external_csr_resolver: Option<ExternalCsrResolverConstructor>,
    listener_pdp: Option<ListenerPdpConstructor>,
    listener_rate_limiter: Option<ListenerRateLimiterConstructor>,
    vault_external_pki: Option<VaultExternalPkiConstructor>,
}

pub(crate) struct CompletedProviderRoles {
    probe_bindings: Vec<runtimeexec::inventory::ProviderProbeBinding>,
}

impl CompletedProviderRoles {
    pub(crate) fn into_probe_bindings(self) -> Vec<runtimeexec::inventory::ProviderProbeBinding> {
        self.probe_bindings
    }
}

impl ProviderRoleBatches {
    pub(crate) fn exact_join(plans: &[assembly_schema::ProviderPlan]) -> anyhow::Result<Self> {
        anyhow::ensure!(
            plans.len() == PROVIDER_CATALOG.len(),
            "deviceidentity RuntimePlan/generated provider catalog count drift"
        );
        let mut batches = Self {
            auth_audit_sink: None,
            device_certificate_store: None,
            device_command_store: None,
            device_mqtt_session: None,
            device_production_artifact_source: None,
            device_revocation_store: None,
            external_csr_resolver: None,
            listener_pdp: None,
            listener_rate_limiter: None,
            vault_external_pki: None,
        };
        for entry in PROVIDER_CATALOG {
            let mut matching = plans
                .iter()
                .filter(|plan| plan.id() == entry.role().as_str());
            let plan = matching.next().ok_or_else(|| {
                anyhow::anyhow!(
                    "deviceidentity RuntimePlan omits generated provider role '{}'",
                    entry.role().as_str()
                )
            })?;
            anyhow::ensure!(
                matching.next().is_none(),
                "deviceidentity RuntimePlan duplicates generated provider role '{}'",
                entry.role().as_str()
            );
            anyhow::ensure!(
                plan.constructor() == entry.evidence().constructor()
                    && plan.outputs() == entry.evidence().outputs(),
                "deviceidentity RuntimePlan disagrees with generated provider role '{}'",
                entry.role().as_str()
            );
            match entry.role() {
                ProviderRole::AuthAuditSink => anyhow::ensure!(
                    batches
                        .auth_audit_sink
                        .replace(AuthAuditSinkConstructor { entry })
                        .is_none(),
                    "deviceidentity generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::DeviceCertificateStore => anyhow::ensure!(
                    batches
                        .device_certificate_store
                        .replace(DeviceCertificateStoreConstructor { entry })
                        .is_none(),
                    "deviceidentity generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::DeviceCommandStore => anyhow::ensure!(
                    batches
                        .device_command_store
                        .replace(DeviceCommandStoreConstructor { entry })
                        .is_none(),
                    "deviceidentity generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::DeviceMqttSession => anyhow::ensure!(
                    batches
                        .device_mqtt_session
                        .replace(DeviceMqttSessionConstructor { entry })
                        .is_none(),
                    "deviceidentity generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::DeviceProductionArtifactSource => anyhow::ensure!(
                    batches
                        .device_production_artifact_source
                        .replace(DeviceProductionArtifactSourceConstructor { entry })
                        .is_none(),
                    "deviceidentity generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::DeviceRevocationStore => anyhow::ensure!(
                    batches
                        .device_revocation_store
                        .replace(DeviceRevocationStoreConstructor { entry })
                        .is_none(),
                    "deviceidentity generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::ExternalCsrResolver => anyhow::ensure!(
                    batches
                        .external_csr_resolver
                        .replace(ExternalCsrResolverConstructor { entry })
                        .is_none(),
                    "deviceidentity generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::ListenerPdp => anyhow::ensure!(
                    batches
                        .listener_pdp
                        .replace(ListenerPdpConstructor { entry })
                        .is_none(),
                    "deviceidentity generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::ListenerRateLimiter => anyhow::ensure!(
                    batches
                        .listener_rate_limiter
                        .replace(ListenerRateLimiterConstructor { entry })
                        .is_none(),
                    "deviceidentity generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                ProviderRole::VaultExternalPki => anyhow::ensure!(
                    batches
                        .vault_external_pki
                        .replace(VaultExternalPkiConstructor { entry })
                        .is_none(),
                    "deviceidentity generated provider role '{}' is duplicated",
                    entry.role().as_str()
                ),
                _ => anyhow::bail!(
                    "deviceidentity generated provider catalog contains unsupported role '{}'",
                    entry.role().as_str()
                ),
            }
        }
        batches.require_complete()?;
        Ok(batches)
    }
    fn require_complete(&self) -> anyhow::Result<()> {
        anyhow::ensure!(
            self.auth_audit_sink.is_some(),
            "deviceidentity generated provider role 'auth-audit-sink' is missing"
        );
        anyhow::ensure!(
            self.device_certificate_store.is_some(),
            "deviceidentity generated provider role 'device-certificate-store' is missing"
        );
        anyhow::ensure!(
            self.device_command_store.is_some(),
            "deviceidentity generated provider role 'device-command-store' is missing"
        );
        anyhow::ensure!(
            self.device_mqtt_session.is_some(),
            "deviceidentity generated provider role 'device-mqtt-session' is missing"
        );
        anyhow::ensure!(
            self.device_production_artifact_source.is_some(),
            "deviceidentity generated provider role 'device-production-artifact-source' is missing"
        );
        anyhow::ensure!(
            self.device_revocation_store.is_some(),
            "deviceidentity generated provider role 'device-revocation-store' is missing"
        );
        anyhow::ensure!(
            self.external_csr_resolver.is_some(),
            "deviceidentity generated provider role 'external-csr-resolver' is missing"
        );
        anyhow::ensure!(
            self.listener_pdp.is_some(),
            "deviceidentity generated provider role 'listener-pdp' is missing"
        );
        anyhow::ensure!(
            self.listener_rate_limiter.is_some(),
            "deviceidentity generated provider role 'listener-rate-limiter' is missing"
        );
        anyhow::ensure!(
            self.vault_external_pki.is_some(),
            "deviceidentity generated provider role 'vault-external-pki' is missing"
        );
        Ok(())
    }
    pub(crate) fn auth_audit_sink(&mut self) -> anyhow::Result<AuthAuditSinkConstructor> {
        self.auth_audit_sink.take().ok_or_else(|| {
            anyhow::anyhow!(
                "deviceidentity provider constructor 'auth-audit-sink' was consumed more than once"
            )
        })
    }
    pub(crate) fn device_certificate_store(
        &mut self,
    ) -> anyhow::Result<DeviceCertificateStoreConstructor> {
        self.device_certificate_store.take().ok_or_else(|| anyhow::anyhow!("deviceidentity provider constructor 'device-certificate-store' was consumed more than once"))
    }
    pub(crate) fn device_command_store(&mut self) -> anyhow::Result<DeviceCommandStoreConstructor> {
        self.device_command_store.take().ok_or_else(|| anyhow::anyhow!("deviceidentity provider constructor 'device-command-store' was consumed more than once"))
    }
    pub(crate) fn device_mqtt_session(&mut self) -> anyhow::Result<DeviceMqttSessionConstructor> {
        self.device_mqtt_session.take().ok_or_else(|| anyhow::anyhow!("deviceidentity provider constructor 'device-mqtt-session' was consumed more than once"))
    }
    pub(crate) fn device_production_artifact_source(
        &mut self,
    ) -> anyhow::Result<DeviceProductionArtifactSourceConstructor> {
        self.device_production_artifact_source.take().ok_or_else(|| anyhow::anyhow!("deviceidentity provider constructor 'device-production-artifact-source' was consumed more than once"))
    }
    pub(crate) fn device_revocation_store(
        &mut self,
    ) -> anyhow::Result<DeviceRevocationStoreConstructor> {
        self.device_revocation_store.take().ok_or_else(|| anyhow::anyhow!("deviceidentity provider constructor 'device-revocation-store' was consumed more than once"))
    }
    pub(crate) fn external_csr_resolver(
        &mut self,
    ) -> anyhow::Result<ExternalCsrResolverConstructor> {
        self.external_csr_resolver.take().ok_or_else(|| anyhow::anyhow!("deviceidentity provider constructor 'external-csr-resolver' was consumed more than once"))
    }
    pub(crate) fn listener_pdp(&mut self) -> anyhow::Result<ListenerPdpConstructor> {
        self.listener_pdp.take().ok_or_else(|| {
            anyhow::anyhow!(
                "deviceidentity provider constructor 'listener-pdp' was consumed more than once"
            )
        })
    }
    pub(crate) fn listener_rate_limiter(
        &mut self,
    ) -> anyhow::Result<ListenerRateLimiterConstructor> {
        self.listener_rate_limiter.take().ok_or_else(|| anyhow::anyhow!("deviceidentity provider constructor 'listener-rate-limiter' was consumed more than once"))
    }
    pub(crate) fn vault_external_pki(&mut self) -> anyhow::Result<VaultExternalPkiConstructor> {
        self.vault_external_pki.take().ok_or_else(|| anyhow::anyhow!("deviceidentity provider constructor 'vault-external-pki' was consumed more than once"))
    }
    #[allow(clippy::too_many_arguments)]
    // reason: generated exact-join closure has one move-only receipt per declared provider role.
    pub(crate) fn finish(
        self,
        inventory: &bootstrap::DomainModuleResult,
        auth_audit_sink: AuthAuditSinkReceipt,
        device_certificate_store: DeviceCertificateStoreReceipt,
        device_command_store: DeviceCommandStoreReceipt,
        device_mqtt_session: DeviceMqttSessionReceipt,
        device_production_artifact_source: DeviceProductionArtifactSourceReceipt,
        device_revocation_store: DeviceRevocationStoreReceipt,
        external_csr_resolver: ExternalCsrResolverReceipt,
        listener_pdp: ListenerPdpReceipt,
        listener_rate_limiter: ListenerRateLimiterReceipt,
        vault_external_pki: VaultExternalPkiReceipt,
    ) -> anyhow::Result<CompletedProviderRoles> {
        let mut staged = [0_usize; 3];
        let mut probe_bindings = Vec::with_capacity(PROVIDER_CATALOG.len());
        let AuthAuditSinkReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = auth_audit_sink;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(
            runtimeexec::inventory::ProviderProbeBinding::from_probe_receipt(
                "auth-audit-sink",
                probe_names,
            )?,
        );
        let DeviceCertificateStoreReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = device_certificate_store;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(
            runtimeexec::inventory::ProviderProbeBinding::from_probe_receipt(
                "device-certificate-store",
                probe_names,
            )?,
        );
        let DeviceCommandStoreReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = device_command_store;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(
            runtimeexec::inventory::ProviderProbeBinding::from_probe_receipt(
                "device-command-store",
                probe_names,
            )?,
        );
        let DeviceMqttSessionReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = device_mqtt_session;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(
            runtimeexec::inventory::ProviderProbeBinding::from_probe_receipt(
                "device-mqtt-session",
                probe_names,
            )?,
        );
        let DeviceProductionArtifactSourceReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = device_production_artifact_source;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(
            runtimeexec::inventory::ProviderProbeBinding::from_probe_receipt(
                "device-production-artifact-source",
                probe_names,
            )?,
        );
        let DeviceRevocationStoreReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = device_revocation_store;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(
            runtimeexec::inventory::ProviderProbeBinding::from_probe_receipt(
                "device-revocation-store",
                probe_names,
            )?,
        );
        let ExternalCsrResolverReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = external_csr_resolver;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(
            runtimeexec::inventory::ProviderProbeBinding::from_probe_receipt(
                "external-csr-resolver",
                probe_names,
            )?,
        );
        let ListenerPdpReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = listener_pdp;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(
            runtimeexec::inventory::ProviderProbeBinding::from_probe_receipt(
                "listener-pdp",
                probe_names,
            )?,
        );
        let ListenerRateLimiterReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = listener_rate_limiter;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(
            runtimeexec::inventory::ProviderProbeBinding::from_probe_receipt(
                "listener-rate-limiter",
                probe_names,
            )?,
        );
        let VaultExternalPkiReceipt {
            probes,
            resources,
            workers,
            probe_names,
        } = vault_external_pki;
        staged[0] += probes;
        staged[1] += resources;
        staged[2] += workers;
        probe_bindings.push(
            runtimeexec::inventory::ProviderProbeBinding::from_probe_receipt(
                "vault-external-pki",
                probe_names,
            )?,
        );
        if self.auth_audit_sink.is_some()
            || self.device_certificate_store.is_some()
            || self.device_command_store.is_some()
            || self.device_mqtt_session.is_some()
            || self.device_production_artifact_source.is_some()
            || self.device_revocation_store.is_some()
            || self.external_csr_resolver.is_some()
            || self.listener_pdp.is_some()
            || self.listener_rate_limiter.is_some()
            || self.vault_external_pki.is_some()
        {
            anyhow::bail!(
                "deviceidentity provider role receipt came from a different exact-join generation"
            );
        }
        anyhow::ensure!(
            inventory.probe_count() == staged[0]
                && inventory.resource_count() == staged[1]
                && inventory.worker_count() == staged[2],
            "deviceidentity transaction provider lifecycle output differs from exact receipts"
        );
        Ok(CompletedProviderRoles { probe_bindings })
    }
}

fn lifecycle_channels(output: &bootstrap::DomainModuleResult) -> Vec<LifecycleChannel> {
    let mut channels = Vec::new();
    if output.probe_count() != 0 {
        channels.push(LifecycleChannel::Probes);
    }
    if output.resource_count() != 0 {
        channels.push(LifecycleChannel::Resources);
    }
    if output.worker_count() != 0 {
        channels.push(LifecycleChannel::Workers);
    }
    channels
}

fn validate_lifecycle_output(
    entry: &ProviderCatalogEntry,
    output: &bootstrap::DomainModuleResult,
) -> anyhow::Result<()> {
    let actual = lifecycle_channels(output);
    anyhow::ensure!(
        actual == entry.evidence().outputs(),
        "deviceidentity provider role '{}' lifecycle mismatch: expected {:?}, actual {:?}",
        entry.role().as_str(),
        entry.evidence().outputs(),
        actual
    );
    Ok(())
}

pub(crate) struct AuthAuditSinkConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct AuthAuditSinkReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl AuthAuditSinkReceipt {
    fn transfer_lifecycle(
        output: bootstrap::DomainModuleResult,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> Self {
        let probe_names = output.probes().map(|(name, _)| name.clone()).collect();
        let receipt = Self {
            probes: output.probe_count(),
            resources: output.resource_count(),
            workers: output.worker_count(),
            probe_names,
        };
        inventory.merge(output);
        receipt
    }
}
pub(crate) struct AuthAuditSinkBatch(bootstrap::DomainModuleResult);
impl AuthAuditSinkConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<AuthAuditSinkBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(AuthAuditSinkBatch(output))
    }
}
impl AuthAuditSinkBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> AuthAuditSinkReceipt {
        AuthAuditSinkReceipt::transfer_lifecycle(self.0, inventory)
    }
}

pub(crate) struct DeviceCertificateStoreConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct DeviceCertificateStoreReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl DeviceCertificateStoreReceipt {
    fn transfer_lifecycle(
        output: bootstrap::DomainModuleResult,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> Self {
        let probe_names = output.probes().map(|(name, _)| name.clone()).collect();
        let receipt = Self {
            probes: output.probe_count(),
            resources: output.resource_count(),
            workers: output.worker_count(),
            probe_names,
        };
        inventory.merge(output);
        receipt
    }
}
pub(crate) struct DeviceCertificateStoreBatch(bootstrap::DomainModuleResult);
impl DeviceCertificateStoreConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<DeviceCertificateStoreBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(DeviceCertificateStoreBatch(output))
    }
}
impl DeviceCertificateStoreBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> DeviceCertificateStoreReceipt {
        DeviceCertificateStoreReceipt::transfer_lifecycle(self.0, inventory)
    }
}

pub(crate) struct DeviceCommandStoreConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct DeviceCommandStoreReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl DeviceCommandStoreReceipt {
    fn transfer_lifecycle(
        output: bootstrap::DomainModuleResult,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> Self {
        let probe_names = output.probes().map(|(name, _)| name.clone()).collect();
        let receipt = Self {
            probes: output.probe_count(),
            resources: output.resource_count(),
            workers: output.worker_count(),
            probe_names,
        };
        inventory.merge(output);
        receipt
    }
}
pub(crate) struct DeviceCommandStoreBatch(bootstrap::DomainModuleResult);
impl DeviceCommandStoreConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<DeviceCommandStoreBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(DeviceCommandStoreBatch(output))
    }
}
impl DeviceCommandStoreBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> DeviceCommandStoreReceipt {
        DeviceCommandStoreReceipt::transfer_lifecycle(self.0, inventory)
    }
}

pub(crate) struct DeviceMqttSessionConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct DeviceMqttSessionReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl DeviceMqttSessionReceipt {
    fn transfer_lifecycle(
        output: bootstrap::DomainModuleResult,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> Self {
        let probe_names = output.probes().map(|(name, _)| name.clone()).collect();
        let receipt = Self {
            probes: output.probe_count(),
            resources: output.resource_count(),
            workers: output.worker_count(),
            probe_names,
        };
        inventory.merge(output);
        receipt
    }
}
pub(crate) struct DeviceMqttSessionBatch(bootstrap::DomainModuleResult);
impl DeviceMqttSessionConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<DeviceMqttSessionBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(DeviceMqttSessionBatch(output))
    }
}
impl DeviceMqttSessionBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> DeviceMqttSessionReceipt {
        DeviceMqttSessionReceipt::transfer_lifecycle(self.0, inventory)
    }
}

pub(crate) struct DeviceProductionArtifactSourceConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct DeviceProductionArtifactSourceReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl DeviceProductionArtifactSourceReceipt {
    fn transfer_lifecycle(
        output: bootstrap::DomainModuleResult,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> Self {
        let probe_names = output.probes().map(|(name, _)| name.clone()).collect();
        let receipt = Self {
            probes: output.probe_count(),
            resources: output.resource_count(),
            workers: output.worker_count(),
            probe_names,
        };
        inventory.merge(output);
        receipt
    }
}
pub(crate) struct DeviceProductionArtifactSourceBatch(bootstrap::DomainModuleResult);
impl DeviceProductionArtifactSourceConstructor {
    pub(crate) fn finish(
        self,
        output: crate::providers::DeviceProductionArtifactSourceCapability,
    ) -> anyhow::Result<DeviceProductionArtifactSourceBatch> {
        let output = output.into_output();
        validate_lifecycle_output(self.entry, &output)?;
        Ok(DeviceProductionArtifactSourceBatch(output))
    }
}
impl DeviceProductionArtifactSourceBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> DeviceProductionArtifactSourceReceipt {
        DeviceProductionArtifactSourceReceipt::transfer_lifecycle(self.0, inventory)
    }
}

pub(crate) struct DeviceRevocationStoreConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct DeviceRevocationStoreReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl DeviceRevocationStoreReceipt {
    fn transfer_lifecycle(
        output: bootstrap::DomainModuleResult,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> Self {
        let probe_names = output.probes().map(|(name, _)| name.clone()).collect();
        let receipt = Self {
            probes: output.probe_count(),
            resources: output.resource_count(),
            workers: output.worker_count(),
            probe_names,
        };
        inventory.merge(output);
        receipt
    }
}
pub(crate) struct DeviceRevocationStoreBatch(bootstrap::DomainModuleResult);
impl DeviceRevocationStoreConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<DeviceRevocationStoreBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(DeviceRevocationStoreBatch(output))
    }
}
impl DeviceRevocationStoreBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> DeviceRevocationStoreReceipt {
        DeviceRevocationStoreReceipt::transfer_lifecycle(self.0, inventory)
    }
}

pub(crate) struct ExternalCsrResolverConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct ExternalCsrResolverReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl ExternalCsrResolverReceipt {
    fn transfer_lifecycle(
        output: bootstrap::DomainModuleResult,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> Self {
        let probe_names = output.probes().map(|(name, _)| name.clone()).collect();
        let receipt = Self {
            probes: output.probe_count(),
            resources: output.resource_count(),
            workers: output.worker_count(),
            probe_names,
        };
        inventory.merge(output);
        receipt
    }
}
pub(crate) struct ExternalCsrResolverBatch(bootstrap::DomainModuleResult);
impl ExternalCsrResolverConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<ExternalCsrResolverBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(ExternalCsrResolverBatch(output))
    }
}
impl ExternalCsrResolverBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> ExternalCsrResolverReceipt {
        ExternalCsrResolverReceipt::transfer_lifecycle(self.0, inventory)
    }
}

pub(crate) struct ListenerPdpConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct ListenerPdpReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl ListenerPdpReceipt {
    fn transfer_lifecycle(
        output: bootstrap::DomainModuleResult,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> Self {
        let probe_names = output.probes().map(|(name, _)| name.clone()).collect();
        let receipt = Self {
            probes: output.probe_count(),
            resources: output.resource_count(),
            workers: output.worker_count(),
            probe_names,
        };
        inventory.merge(output);
        receipt
    }
}
pub(crate) struct ListenerPdpBatch(bootstrap::DomainModuleResult);
impl ListenerPdpConstructor {
    pub(crate) fn finish(
        self,
        output: ListenerPdpJwksLifecycle,
    ) -> anyhow::Result<ListenerPdpBatch> {
        let output = output.into_output();
        validate_lifecycle_output(self.entry, &output)?;
        Ok(ListenerPdpBatch(output))
    }
}
impl ListenerPdpBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> ListenerPdpReceipt {
        ListenerPdpReceipt::transfer_lifecycle(self.0, inventory)
    }
}

pub(crate) struct ListenerRateLimiterConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct ListenerRateLimiterReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl ListenerRateLimiterReceipt {
    fn transfer_lifecycle(
        output: bootstrap::DomainModuleResult,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> Self {
        let probe_names = output.probes().map(|(name, _)| name.clone()).collect();
        let receipt = Self {
            probes: output.probe_count(),
            resources: output.resource_count(),
            workers: output.worker_count(),
            probe_names,
        };
        inventory.merge(output);
        receipt
    }
}
pub(crate) struct ListenerRateLimiterBatch {
    lifecycle: bootstrap::DomainModuleResult,
    value: redis::RedisRateLimiter,
}
#[cfg(test)]
pub(crate) struct ListenerRateLimiterBatchForTest(bootstrap::DomainModuleResult);
impl ListenerRateLimiterConstructor {
    pub(crate) fn finish(
        self,
        output: redis::RedisRateLimiterCapability,
    ) -> anyhow::Result<ListenerRateLimiterBatch> {
        let value = output.into_limiter();
        let lifecycle = bootstrap::DomainModuleResult::default();
        validate_lifecycle_output(self.entry, &lifecycle)?;
        Ok(ListenerRateLimiterBatch { lifecycle, value })
    }
}
#[cfg(test)]
impl ListenerRateLimiterConstructor {
    pub(crate) fn finish_for_test(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<ListenerRateLimiterBatchForTest> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(ListenerRateLimiterBatchForTest(output))
    }
}
impl ListenerRateLimiterBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> (ListenerRateLimiterReceipt, redis::RedisRateLimiter) {
        (
            ListenerRateLimiterReceipt::transfer_lifecycle(self.lifecycle, inventory),
            self.value,
        )
    }
}
#[cfg(test)]
impl ListenerRateLimiterBatchForTest {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> ListenerRateLimiterReceipt {
        ListenerRateLimiterReceipt::transfer_lifecycle(self.0, inventory)
    }
}

pub(crate) struct VaultExternalPkiConstructor {
    entry: &'static ProviderCatalogEntry,
}
pub(crate) struct VaultExternalPkiReceipt {
    probes: usize,
    resources: usize,
    workers: usize,
    probe_names: Vec<primitives::ProbeName>,
}
impl VaultExternalPkiReceipt {
    fn transfer_lifecycle(
        output: bootstrap::DomainModuleResult,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> Self {
        let probe_names = output.probes().map(|(name, _)| name.clone()).collect();
        let receipt = Self {
            probes: output.probe_count(),
            resources: output.resource_count(),
            workers: output.worker_count(),
            probe_names,
        };
        inventory.merge(output);
        receipt
    }
}
pub(crate) struct VaultExternalPkiBatch(bootstrap::DomainModuleResult);
impl VaultExternalPkiConstructor {
    pub(crate) fn finish(
        self,
        output: bootstrap::DomainModuleResult,
    ) -> anyhow::Result<VaultExternalPkiBatch> {
        validate_lifecycle_output(self.entry, &output)?;
        Ok(VaultExternalPkiBatch(output))
    }
}
impl VaultExternalPkiBatch {
    pub(crate) fn transfer(
        self,
        inventory: &mut bootstrap::DomainModuleResult,
    ) -> VaultExternalPkiReceipt {
        VaultExternalPkiReceipt::transfer_lifecycle(self.0, inventory)
    }
}

warning: diport DTO 字段不得持裸字节缓冲（Vec<u8> / [u8;N] / Box<[u8]> 或其 Option 包装）：改用 canonical redacted carrier（Debug/Display 脱敏）
  --> $DIR/diport.rs:9:8
   |
LL |     f: Vec<u8>,
   |        ^^^^^^^
   |
   = help: 动态 payload 改用 `RedactedBytes`，固定宽度结构字节改用 crate-internal `RedactedFixedBytes<N>`（均位于 `crates/diport/src/redacted_bytes.rs`，`Debug`/`Display` 脱敏）；极少数公开字节（如 CRL 序列号）或已 `derive(secure::Redact)` 治理的字段，三步同步：加 `is_structural_carve_out` 名单 + ADR-013 §4 carve-out registry + `ui/diport.rs` 加绿例（生产代码不用 `#[allow]`——dylint 未加载时触发 `unknown_lints`、`-D warnings` 红）
   = note: `#[warn(rss_diport_dto_debug_redacted)]` on by default

warning: diport DTO 字段不得持裸字节缓冲（Vec<u8> / [u8;N] / Box<[u8]> 或其 Option 包装）：改用 canonical redacted carrier（Debug/Display 脱敏）
  --> $DIR/diport.rs:14:8
   |
LL |     f: [u8; 32],
   |        ^^^^^^^^
   |
   = help: 动态 payload 改用 `RedactedBytes`，固定宽度结构字节改用 crate-internal `RedactedFixedBytes<N>`（均位于 `crates/diport/src/redacted_bytes.rs`，`Debug`/`Display` 脱敏）；极少数公开字节（如 CRL 序列号）或已 `derive(secure::Redact)` 治理的字段，三步同步：加 `is_structural_carve_out` 名单 + ADR-013 §4 carve-out registry + `ui/diport.rs` 加绿例（生产代码不用 `#[allow]`——dylint 未加载时触发 `unknown_lints`、`-D warnings` 红）

warning: diport DTO 字段不得持裸字节缓冲（Vec<u8> / [u8;N] / Box<[u8]> 或其 Option 包装）：改用 canonical redacted carrier（Debug/Display 脱敏）
  --> $DIR/diport.rs:19:8
   |
LL |     f: Box<[u8]>,
   |        ^^^^^^^^^
   |
   = help: 动态 payload 改用 `RedactedBytes`，固定宽度结构字节改用 crate-internal `RedactedFixedBytes<N>`（均位于 `crates/diport/src/redacted_bytes.rs`，`Debug`/`Display` 脱敏）；极少数公开字节（如 CRL 序列号）或已 `derive(secure::Redact)` 治理的字段，三步同步：加 `is_structural_carve_out` 名单 + ADR-013 §4 carve-out registry + `ui/diport.rs` 加绿例（生产代码不用 `#[allow]`——dylint 未加载时触发 `unknown_lints`、`-D warnings` 红）

warning: diport DTO 字段不得持裸字节缓冲（Vec<u8> / [u8;N] / Box<[u8]> 或其 Option 包装）：改用 canonical redacted carrier（Debug/Display 脱敏）
  --> $DIR/diport.rs:24:8
   |
LL |     f: Option<Vec<u8>>,
   |        ^^^^^^^^^^^^^^^
   |
   = help: 动态 payload 改用 `RedactedBytes`，固定宽度结构字节改用 crate-internal `RedactedFixedBytes<N>`（均位于 `crates/diport/src/redacted_bytes.rs`，`Debug`/`Display` 脱敏）；极少数公开字节（如 CRL 序列号）或已 `derive(secure::Redact)` 治理的字段，三步同步：加 `is_structural_carve_out` 名单 + ADR-013 §4 carve-out registry + `ui/diport.rs` 加绿例（生产代码不用 `#[allow]`——dylint 未加载时触发 `unknown_lints`、`-D warnings` 红）

warning: diport DTO 字段不得持裸字节缓冲（Vec<u8> / [u8;N] / Box<[u8]> 或其 Option 包装）：改用 canonical redacted carrier（Debug/Display 脱敏）
  --> $DIR/diport.rs:32:20
   |
LL |             pub f: Vec<u8>,
   |                    ^^^^^^^
   |
   = help: 动态 payload 改用 `RedactedBytes`，固定宽度结构字节改用 crate-internal `RedactedFixedBytes<N>`（均位于 `crates/diport/src/redacted_bytes.rs`，`Debug`/`Display` 脱敏）；极少数公开字节（如 CRL 序列号）或已 `derive(secure::Redact)` 治理的字段，三步同步：加 `is_structural_carve_out` 名单 + ADR-013 §4 carve-out registry + `ui/diport.rs` 加绿例（生产代码不用 `#[allow]`——dylint 未加载时触发 `unknown_lints`、`-D warnings` 红）

warning: diport DTO 字段不得持裸字节缓冲（Vec<u8> / [u8;N] / Box<[u8]> 或其 Option 包装）：改用 canonical redacted carrier（Debug/Display 脱敏）
  --> $DIR/diport.rs:35:59
   |
LL |         pub struct RedactedFixedBytes<const N: usize>(pub [u8; N]);
   |                                                           ^^^^^^^
   |
   = help: 动态 payload 改用 `RedactedBytes`，固定宽度结构字节改用 crate-internal `RedactedFixedBytes<N>`（均位于 `crates/diport/src/redacted_bytes.rs`，`Debug`/`Display` 脱敏）；极少数公开字节（如 CRL 序列号）或已 `derive(secure::Redact)` 治理的字段，三步同步：加 `is_structural_carve_out` 名单 + ADR-013 §4 carve-out registry + `ui/diport.rs` 加绿例（生产代码不用 `#[allow]`——dylint 未加载时触发 `unknown_lints`、`-D warnings` 红）

warning: diport DTO 字段不得持裸字节缓冲（Vec<u8> / [u8;N] / Box<[u8]> 或其 Option 包装）：改用 canonical redacted carrier（Debug/Display 脱敏）
  --> $DIR/diport.rs:90:31
   |
LL |     pub struct CertSerial(pub Vec<u8>);
   |                               ^^^^^^^
   |
   = help: 动态 payload 改用 `RedactedBytes`，固定宽度结构字节改用 crate-internal `RedactedFixedBytes<N>`（均位于 `crates/diport/src/redacted_bytes.rs`，`Debug`/`Display` 脱敏）；极少数公开字节（如 CRL 序列号）或已 `derive(secure::Redact)` 治理的字段，三步同步：加 `is_structural_carve_out` 名单 + ADR-013 §4 carve-out registry + `ui/diport.rs` 加绿例（生产代码不用 `#[allow]`——dylint 未加载时触发 `unknown_lints`、`-D warnings` 红）

warning: diport DTO 字段不得持裸字节缓冲（Vec<u8> / [u8;N] / Box<[u8]> 或其 Option 包装）：改用 canonical redacted carrier（Debug/Display 脱敏）
  --> $DIR/diport.rs:91:35
   |
LL |     pub struct SecretMaterial(pub Vec<u8>);
   |                                   ^^^^^^^
   |
   = help: 动态 payload 改用 `RedactedBytes`，固定宽度结构字节改用 crate-internal `RedactedFixedBytes<N>`（均位于 `crates/diport/src/redacted_bytes.rs`，`Debug`/`Display` 脱敏）；极少数公开字节（如 CRL 序列号）或已 `derive(secure::Redact)` 治理的字段，三步同步：加 `is_structural_carve_out` 名单 + ADR-013 §4 carve-out registry + `ui/diport.rs` 加绿例（生产代码不用 `#[allow]`——dylint 未加载时触发 `unknown_lints`、`-D warnings` 红）

warning: diport DTO 字段不得持裸字节缓冲（Vec<u8> / [u8;N] / Box<[u8]> 或其 Option 包装）：改用 canonical redacted carrier（Debug/Display 脱敏）
  --> $DIR/diport.rs:92:36
   |
LL |     pub struct ArchiveChecksum(pub [u8; 32]);
   |                                    ^^^^^^^^
   |
   = help: 动态 payload 改用 `RedactedBytes`，固定宽度结构字节改用 crate-internal `RedactedFixedBytes<N>`（均位于 `crates/diport/src/redacted_bytes.rs`，`Debug`/`Display` 脱敏）；极少数公开字节（如 CRL 序列号）或已 `derive(secure::Redact)` 治理的字段，三步同步：加 `is_structural_carve_out` 名单 + ADR-013 §4 carve-out registry + `ui/diport.rs` 加绿例（生产代码不用 `#[allow]`——dylint 未加载时触发 `unknown_lints`、`-D warnings` 红）

warning: diport DTO 字段不得持裸字节缓冲（Vec<u8> / [u8;N] / Box<[u8]> 或其 Option 包装）：改用 canonical redacted carrier（Debug/Display 脱敏）
  --> $DIR/diport.rs:93:36
   |
LL |     pub struct PkiPolicyDigest(pub [u8; 32]);
   |                                    ^^^^^^^^
   |
   = help: 动态 payload 改用 `RedactedBytes`，固定宽度结构字节改用 crate-internal `RedactedFixedBytes<N>`（均位于 `crates/diport/src/redacted_bytes.rs`，`Debug`/`Display` 脱敏）；极少数公开字节（如 CRL 序列号）或已 `derive(secure::Redact)` 治理的字段，三步同步：加 `is_structural_carve_out` 名单 + ADR-013 §4 carve-out registry + `ui/diport.rs` 加绿例（生产代码不用 `#[allow]`——dylint 未加载时触发 `unknown_lints`、`-D warnings` 红）

warning: diport DTO 字段不得持裸字节缓冲（Vec<u8> / [u8;N] / Box<[u8]> 或其 Option 包装）：改用 canonical redacted carrier（Debug/Display 脱敏）
  --> $DIR/diport.rs:94:34
   |
LL |     pub struct PkiSpkiDigest(pub [u8; 32]);
   |                                  ^^^^^^^^
   |
   = help: 动态 payload 改用 `RedactedBytes`，固定宽度结构字节改用 crate-internal `RedactedFixedBytes<N>`（均位于 `crates/diport/src/redacted_bytes.rs`，`Debug`/`Display` 脱敏）；极少数公开字节（如 CRL 序列号）或已 `derive(secure::Redact)` 治理的字段，三步同步：加 `is_structural_carve_out` 名单 + ADR-013 §4 carve-out registry + `ui/diport.rs` 加绿例（生产代码不用 `#[allow]`——dylint 未加载时触发 `unknown_lints`、`-D warnings` 红）

warning: diport DTO 字段不得持裸字节缓冲（Vec<u8> / [u8;N] / Box<[u8]> 或其 Option 包装）：改用 canonical redacted carrier（Debug/Display 脱敏）
  --> $DIR/diport.rs:95:35
   |
LL |     pub struct PkiChainDigest(pub [u8; 32]);
   |                                   ^^^^^^^^
   |
   = help: 动态 payload 改用 `RedactedBytes`，固定宽度结构字节改用 crate-internal `RedactedFixedBytes<N>`（均位于 `crates/diport/src/redacted_bytes.rs`，`Debug`/`Display` 脱敏）；极少数公开字节（如 CRL 序列号）或已 `derive(secure::Redact)` 治理的字段，三步同步：加 `is_structural_carve_out` 名单 + ADR-013 §4 carve-out registry + `ui/diport.rs` 加绿例（生产代码不用 `#[allow]`——dylint 未加载时触发 `unknown_lints`、`-D warnings` 红）

warning: 12 warnings emitted


# Scope boundary

This spec is limited to four tracker PBIs. #2035 does not add runtime configuration, migrations,
durable trace metadata, metrics, or an operations backend. PostgreSQL pool observation, exporter
work, and consumer settlement remain deferred triggers recorded in
`contracts/deferred-observability-triggers.yaml`.

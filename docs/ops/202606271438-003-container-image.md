# RSS server 容器镜像与部署

> #1134（area-tooling）。本文档是 `bins/server` 多阶段 Dockerfile + 最小运行镜像的构建 / 运行 / 验收 /
> 编排契约。镜像构建逻辑单源 = 仓库根 `Dockerfile`；演示栈 = `deploy/docker-compose.yml`；冒烟 acceptance
> harness = `deploy/smoke.sh`（`make docker-smoke`）。必填环境变量单源 = `assemblies/runtime/src/lib.rs`
> 的 `build_*` fail-fast 读取点。

## 背景

RSS 此前零部署产物（无 Dockerfile / compose / k8s）。本文档随第一份容器化交付单元落地：把
`bins/server`（薄入口 → `runtime::run()`）编进**非 root、只读 rootfs 友好、distroless** 的最小运行镜像，
并给出可机器闭环验收的演示栈（`docker run → /readyz 200`；#1500 引入 SPIFFE/mTLS 后，
readyz 200 smoke 的 SPIRE fixture 由 backlog #1649 补齐）。

> **scaffold 阶段声明**：当前 runtime compose `SettingsDomain` + `AuditDomain`。对外**匿名可验收面**是
> `/health/v1/*`（healthz/readyz/**metrics**，Health listener 8083，`AuthScheme::NoAuth`）；此外 `AuditDomain` 在
> **Admin listener（8082）**注册 `GET /api/v1/audit/entries`（按 Admin listener 鉴权保护，非匿名可达）。其余业务
> 路由待 #1017 伞 / 各域 wire PR。
>
> **匿名 metrics 面的网络边界（部署契约，非仅代码约定）**：`/health/v1/metrics` 与 healthz/readyz 同挂
> `NoAuth` Health listener（`diport::MetricsExporter` 规约要求 scrape 面仅内部网络可达）——**必须仅 loopback /
> cluster-internal 暴露，绝不作公网 ingress 入口**。演示栈已在 `deploy/docker-compose.yml` 把 8083 绑 `127.0.0.1`
> 落实该边界；生产 k8s 经 Health listener 的 `ClusterIP` Service + Prometheus cluster-internal scrape 保持同语义（见 §可观测性导出）。
> 镜像真实启动、连 PG、跑迁移、serve readyz 是容器化交付链的验收目标；#1500 之后完整 readyz 200 smoke
> 还需要 SPIRE fixture（#1649）补齐。业务端点随后续域 PR 进入同一镜像，Dockerfile 不变。

## 镜像结构（多阶段 cargo-chef + distroless）

`Dockerfile` 4 阶段（ref: cargo-chef README.md / ref: distroless examples/rust/Dockerfile）：

| 阶段 | 基底 | 职责 |
|------|------|------|
| `chef` | `rust:1.96.0-bookworm` | 钉版工具链（= `rust-toolchain.toml` channel）+ `cargo-chef`（钉 0.1.77；须 ≥0.1.71 支持 edition 2024）。 |
| `planner` | `chef` | `cargo chef prepare` → `recipe.json`（仅依赖图，**不含业务源** ⇒ 改代码不失效）。 |
| `builder` | `chef` | `cargo chef cook --release --locked --bin server --bin rss`（依赖层，缓存命中点）→ `cargo build --release --locked --bin server --bin rss` → `strip`。 |
| `runtime` | `gcr.io/distroless/cc-debian12:nonroot` | `COPY` server 入口二进制与 rss 维护二进制；非 root uid 65532；无 shell / 包管理器。 |

**为什么 distroless/cc**：server 二进制 glibc 动态链接、TLS 走 rustls+ring（ring 静态、**无 OpenSSL/
native-tls 动态依赖**），运行期只需 glibc + libgcc + ca-certificates——`cc-debian12` 恰好提供，再无多余攻击面。

## 构建

```bash
docker build -t rss-server:dev .   # 仓库根；或 make docker-build
```

`.dockerignore` 把上下文裁到「cargo 解析整个 workspace + 编 `--bin server` 子图」所需的最小集（排除
`target/`、`lints/`〔独立 workspace〕、`contracts/`〔仅 xtask codegen 输入，无 build.rs 消费〕、docs/ 等）。

## 运行：必填环境变量

server 对**缺失的必填变量 fail-fast**（启动即 `Err` 退出、错误含变量名，不静默 noop）。完整演示值见
`deploy/.env.example`。

| 变量 | 必填 | 语义 |
|------|------|------|
| `RSS_PRIMARY_LISTEN_ADDR` / `RSS_INTERNAL_LISTEN_ADDR` / `RSS_ADMIN_LISTEN_ADDR` / `RSS_HEALTH_LISTEN_ADDR` | ✔ | 4 个 listener bind 地址（`host:port`，无默认）。容器内须 `0.0.0.0:<port>` 才可经端口映射达宿主。 |
| `RSS_LISTENER_ALLOW_PLAINTEXT` | 可选 | Listener 明文 dev/test opt-in。缺省 / `false` / `0` / `no` 均拒绝 plaintext listener bind；`true` / `1` / `yes` 只放行 loopback；`dev-container` 放行 loopback 与 compose 演示 wildcard bind（`0.0.0.0` / `[::]`）。Internal 默认 mTLS 不受此明文门约束；Internal `service-token` 本机测试路径受此明文门约束且只能 loopback。非法值启动 fail-fast。详见 §Listener 明文策略。 |
| `RSS_INTERNAL_AUTH_SCHEME` | 可选 | Internal listener 认证方案。缺省 `mtls`；显式 `service-token` 仅允许 loopback 本机测试。非 loopback Internal listener 必须使用 mTLS。非法值启动 fail-fast。 |
| `RSS_INTERNAL_MTLS_SPIFFE_ALLOW_SET` | ✔(Internal `mtls`) | Internal inbound mTLS exact SPIFFE ID allow-set（逗号分隔，非空、canonical、无 wildcard）。 |
| `SPIFFE_ENDPOINT_SOCKET` | 可选(SPIRE) | SPIRE Workload API endpoint。未设时由 `spiffe` crate 使用其默认 endpoint/env 解析；不可用会使 inbound/outbound mTLS 构造启动 fail-fast。 |
| `RSS_OIDC_ISSUER` / `RSS_OIDC_AUDIENCE` / `RSS_OIDC_TRUSTED_KINDS` | ✔ | JWT 验签 IdP；`TRUSTED_KINDS` 逗号分隔 ≥1（空则所有 JWT 401）。 |
| `RSS_OIDC_JWKS_PATH` | ✔ | 本地 JWKS 文件源。启动期必须可读且至少含一把可用 ES256/HS256 key；刷新失败保留 last-good keyset，但 `oidc_jwks_ready` 降为 unhealthy。 |
| `RSS_OIDC_JWKS_REFRESH_INTERVAL_SECS` | 可选 | JWKS 文件刷新周期秒，默认 `60`，范围 `5..=3600`；非法值启动 fail-fast。 |
| `RSS_PG_HOST` / `RSS_PG_PORT` / `RSS_PG_DATABASE` / `RSS_PG_USERNAME` / `RSS_PG_PASSWORD` | ✔ | PostgreSQL serving 连接参数。长期连接角色必须直连 `rss_app`，且 `rss_app` 必须是非 superuser、NOBYPASSRLS；`deploy/postgres-init/` 仅为 demo 给固定 serving role 配 LOGIN。 |
| `RSS_PG_MIGRATOR_USERNAME` / `RSS_PG_MIGRATOR_PASSWORD` | ✔ | PostgreSQL migrator 凭据。启动期只用于 `run_migrations`，不进入长期 serving pool。 |
| `RSS_PG_SSL_MODE` | 可选 | TLS 模式（`disable`/`allow`/`prefer`/`require`/`verify-ca`/`verify-full`，大小写不敏感）。**缺省 / 非法 / 空 → `verify-full`**（零信任默认）。详见 §PG TLS。 |
| `RSS_PG_SSL_ROOT_CERT_PATH` | 可选 | PostgreSQL 私有 CA 根证书路径。未设时使用 sqlx/rustls 公共根行为；设置后必须是非空、可读文件，并同时用于 serving 与 migrator `PgConfig::with_ssl_root_cert`。非法路径启动 fail-fast。详见 §PG TLS。 |
| `RSS_PG_READINESS_SAMPLE_INTERVAL_SECS` | 可选 | configs_ready DB 采样周期秒（默认 5，范围 1..=300）。 |
| `RSS_SESSION_SWEEP_INTERVAL_MS` | 可选 | `session_sweeper` 过期会话清理周期毫秒（默认 300000，最小 1000）。误配 fail-soft（warn + 默认）。 |
| `RSS_SETTINGS_ALLOW_LEGACY_PLAINTEXT_CONFIG_VALUES` | 可选 | 临时安全豁免。缺省 / `false` / `0` / `no` 均拒绝启动期发现的 `config_entries.protection_scheme=0` 行；`true` / `1` / `yes` 仅在 backfill 前短期放行。非法值启动 fail-fast。 |
| `RSS_REDIS_URL` | ✔ | Redis runtime URL（distributed lock provider + `redis_ready` readiness）。缺失或启动期 PING 失败均 fail-fast；运行期 Redis PING 失败会使 `/readyz` 聚合为 503。默认只接受 `rediss://` + AUTH/TLS；`redis://` 仅显式明文策略允许：`true` 只放行 loopback，`dev-container` 只给 compose 演示栈。 |
| `RSS_REDIS_ALLOW_PLAINTEXT` | 可选 | Redis 明文 dev/test opt-in。缺省 / `false` / `0` / `no` 均拒绝 `redis://`；`true` / `1` / `yes` 只放行 loopback `redis://`；`dev-container` 放行 compose 演示栈服务名；非法值启动 fail-fast。 |
| `RSS_REDIS_READINESS_SAMPLE_INTERVAL_SECS` | 可选 | redis_ready PING 采样周期秒（默认 5，范围 1..=30）。 |
| `RSS_S3_ENDPOINT_URL` / `RSS_S3_BUCKET` / `RSS_S3_ACCESS_KEY_ID` / `RSS_S3_SECRET_ACCESS_KEY` | ✔ | S3 object-store runtime consumer 配置。runtime 启动期构造真实 AWS SDK S3 client；`s3_object_store_ready` canary 周期执行 put/get/delete/get-miss，失败会使 `/readyz` 聚合为 503。缺失或 bucket/endpoint 误配 fail-fast。 |
| `RSS_S3_REGION` / `RSS_S3_SESSION_TOKEN` / `RSS_S3_FORCE_PATH_STYLE` | 可选 | S3 region 默认 `us-east-1`；session token 可选；MinIO/S3-compatible endpoint 通常设 `RSS_S3_FORCE_PATH_STYLE=true`，生产 AWS S3 默认 false。 |
| `RSS_S3_ALLOW_PLAINTEXT` | 可选 | S3 明文 dev/test opt-in。缺省 / `false` / `0` / `no` 均拒绝 `http://`；`true` / `1` / `yes` 只放行 loopback `http://`；`dev-container` 只放行 compose 演示栈服务名 `minio`。生产须使用 `https://`，由 runtime 显式 rustls+ring HTTP client 接线。 |
| `RSS_S3_CANARY_KEY_PREFIX` / `RSS_S3_CANARY_INTERVAL_SECS` / `RSS_S3_CANARY_TIMEOUT_SECS` | 可选 | S3 readiness canary key 前缀（默认 `rss/runtime-canary`）、周期（默认 60s，范围 1..=300）、单轮超时（默认 5s，范围 1..=60，且必须 ≤ 周期）。每实例 key 带 UUID，避免多副本互删。 |
| `RSS_OTEL_ENDPOINT` | 可选 | OTLP/gRPC trace 导出 collector 端点（**按需开启**）。**未设 → 仅 fmt 日志、不导出 trace**；`https://…` → TLS（生产默认）；`http://…` → 仅 loopback host 明文 opt-in（非 loopback / 非 http(s) scheme → **启动 fail-fast**，错误只含变量名）。详见 §可观测性导出。 |
| `RSS_AUDIT_CHAIN_KEY_B64URL` | ✔ | 审计链 HMAC key（base64url，解码 ≥32B）。缺失 / 弱 key fail-fast。 |
| `RSS_VAULT_ADDR` / `RSS_VAULT_TOKEN` | ✔ | vault secret resolver + Transit `Signer`/settings ConfigValue `KeyProvider` 配置。resolver 启动期不联网（空 allowlist）；**vault `Signer` 在每次 login / refresh 时真实请求 Transit 签 access JWT（#1252）**，settings 启动期对 ConfigValue key 做 encrypt+decrypt self-check（#1477）——client 经 rustls（ring + webpki-roots，#1252 已接 TLS）加密出网，生产须**可达 vault + 有效证书链**。`ADDR` 须 `https://`（fail-fast 拒 http）；compose 演示栈使用 Vault `-dev-tls` + generated CA。 |
| `RSS_VAULT_CA_CERT_PEM_PATH` | 可选 | 额外信任的 Vault HTTPS CA PEM bundle 路径。用于私有 CA / compose dev-tls；缺省只信 webpki roots。设置后空值、不可读路径或非法 PEM bundle 均启动 fail-fast；错误只含变量名/路径语义，不打印证书内容。 |
| `RSS_VAULT_TRANSIT_MOUNT` | ✔ | vault Transit 引擎 mount path（如 `transit`；access JWT 签发 + settings ConfigValue at-rest encryption 复用）。 |
| `RSS_SETTINGS_CONFIG_VALUE_KEY_NAME` | ✔ | settings `ConfigValue` at-rest encryption 的 Vault Transit key 名（#1477）。该 Transit key 必须 `derived=true`，因为 RSS 把 `DerivedAad` 绑定到 Vault `context` 以保留原生 rewrap；启动 self-check 会验证 wrong-AAD 解密 fail-closed。缺失 / 非法 key name 启动 fail-fast；运行期 `keyprovider_ready` sampler 失败会使 `/readyz` 聚合为 503。 |
| `RSS_KEYPROVIDER_READINESS_SAMPLE_INTERVAL_SECS` | 可选 | keyprovider_ready Vault Transit encrypt/decrypt 采样周期秒（默认 5，范围 1..=30）。 |
| `RSS_JWT_ISSUER` / `RSS_JWT_AUDIENCE` | ✔ | 签发的 access JWT `iss` / `aud` claim（须与 `RSS_OIDC_ISSUER` / `RSS_OIDC_AUDIENCE` 一致，否则自签自验失败）。 |
| `RSS_JWT_ES256_KEY_ID` | ✔ | vault Transit sign key 名 = JOSE `kid`；验签侧 JWKS 文件必须包含对应公钥，二者须运维一致接线，OIDC-ALG-KEYPATH-01。 |
| `RSS_JWT_ACCESS_TTL_SECS` | ✔ | access JWT 有效期秒（`exp = iat + ttl`，须 > 0）。 |
| `RSS_REFRESH_TTL_SECS` | 可选 | refresh token 有效期秒（默认 30 天，须 > 0 且 ≤ 上限）。 |
| `RSS_TOPOLOGY` | ✔ | 事件传输拓扑（#1251）：`durable-shared`（per-domain AMQP，缺则回退共享 URL）/ `durable-isolated`（per-domain 必填、禁回退）。**生产 runtime 不接受 `demo`**（in-mem 不可达，启动 fail-fast）。缺失 fail-fast。 |
| `RSS_<DOMAIN>_AMQP_URL`（如 `RSS_IDENTITY_AMQP_URL`） | ✔(durable，与下行二选一) | per-domain AMQP broker URL（生产 `amqps://user:pass@host/vhost`，**优先**于共享回退）。默认拒绝 `amqp://`；明文只允许显式策略：`true` 只放行 loopback，`dev-container` 只给 compose 演示栈。relay 发布 + consumer 订阅共用此 vhost。 |
| `RSS_AMQP_URL` | ✔(durable，与上行二选一) | 共享回退 AMQP broker URL：`durable-shared` 缺 per-domain 时回退此；`durable-isolated` 配此即 **fail-closed**（禁回退共享凭据）。配置必须覆盖 runtime required domains（relay 发布域 + consumer 订阅 topic owner）：要么为每个 required domain 配 `RSS_<DOMAIN>_AMQP_URL`，要么在 `durable-shared` 提供 `RSS_AMQP_URL` 作为共享 fallback；否则 `eventtransport::resolve` 启动 fail-closed。 |
| `RSS_AMQP_ALLOW_PLAINTEXT` | 可选 | AMQP 明文 dev/test opt-in。缺省 / `false` / `0` / `no` 均拒绝 `amqp://`；`true` / `1` / `yes` 只放行 loopback `amqp://`；`dev-container` 放行 compose 演示栈服务名；非法值启动 fail-fast。 |
| `RSS_DOMAIN_TRANSPORT_REQUIRED_DOMAINS` | ✔ | outbound synchronous domain transport required domains（逗号分隔，非空）。每个 domain 都必须解析出 endpoint 和 mTLS policy。 |
| `RSS_<DOMAIN>_DOMAIN_TRANSPORT_URL` / `RSS_DOMAIN_TRANSPORT_URL` | ✔(与 topology 相关) | outbound domain HTTP endpoint。`durable-shared` 可用共享 `RSS_DOMAIN_TRANSPORT_URL` fallback；`durable-isolated` 禁 shared fallback，必须逐 domain 配 `RSS_<DOMAIN>_DOMAIN_TRANSPORT_URL`。endpoint 必须是 `https://`，且不得含 userinfo/query/fragment。 |
| `RSS_DOMAIN_TRANSPORT_MTLS_LOCAL_SPIFFE_ID` | ✔ | outbound mTLS client 本地 workload SPIFFE ID。启动时会校验 SPIRE 当前 X.509-SVID 与该值一致。 |
| `RSS_<DOMAIN>_DOMAIN_TRANSPORT_MTLS_SPIFFE_ALLOW_SET` | ✔(每个 required domain) | outbound server exact SPIFFE ID allow-set（逗号分隔，非空、canonical、无 wildcard）。runtime 从 allow-set 派生 trust-domain allow-list；server ID 不在允许 trust domain 内会启动失败。 |
| `RSS_TENANT_AUTHORITY_HMAC_KEY_B64URL` | ✔(durable) | outbox relay 签发 `tenantAuthority` 的 HMAC-SHA256 key（base64url no-pad，解码后 ≥32B）。缺失、弱 key 或非 base64url 均 fail-fast；生产经密管下发并轮换。 |
| `RSS_TENANT_AUTHORITY_TTL_SECS` / `RSS_TENANT_AUTHORITY_CLOCK_SKEW_SECS` | 可选 | authority token TTL 与跨实例时钟偏差容忍，默认 `3600` / `60` 秒；clock skew 上限 300 秒，超限 fail-fast。运行环境仍必须启用 NTP，clock skew 只是防正常 relay/consumer 小偏差。 |
| `RSS_DLX_PAYLOAD_KEY_NAME` | ✔(durable) | DLX `dead_letter.original_entry` 加密用 Vault Transit key name；与 `RSS_VAULT_ADDR` / `RSS_VAULT_TOKEN` / `RSS_VAULT_TRANSIT_MOUNT` 共同构成 KeyProvider。缺失或非法 key name fail-fast。 |
| `RSS_RELAY_POLL_INTERVAL_MS` / `RSS_RELAY_BATCH_SIZE` / `RSS_RELAY_SAMPLE_INTERVAL_MS` | 可选 | outbox relay 调优（默认 200ms / 16 / 30000ms）。误配 fail-soft（warn + 默认）。 |

### Listener 明文策略

HTTP listener 明文 bind 默认 fail-closed。`RSS_LISTENER_ALLOW_PLAINTEXT=true` 仅用于本机 dev/test，只允许
`127.0.0.1` / `::1` loopback bind；`RSS_LISTENER_ALLOW_PLAINTEXT=dev-container` 只用于 compose 演示栈，额外允许
`0.0.0.0` / `[::]` wildcard bind 供端口映射使用，仍拒绝固定非 loopback 地址。Primary / Admin / Health listener
以及 Internal `service-token` 本机测试路径都属于 plaintext listener；Internal 默认 `mtls` 由 SPIFFE/rustls listener
承载，不受明文 opt-in 门约束。

Internal `service-token` 只保留为 loopback 本机测试路径。若绑定到 `0.0.0.0` / `[::]` 或固定非 loopback 地址，
启动直接 fail-fast；旧 `RSS_INTERNAL_SERVICE_TOKEN_MIGRATION_*` env 不再放行非 loopback 绑定。生产 Internal listener
必须使用 SPIFFE/mTLS。

### OIDC JWKS 文件源

serving runtime 的 JWT 验签 key 只从 `RSS_OIDC_JWKS_PATH` 指向的本地 JWKS 文件读取。文件在启动期必须存在、
可读且至少包含一把可用 ES256/HS256 key；`RSS_OIDC_JWKS_REFRESH_INTERVAL_SECS` 控制后台重读周期。刷新失败不会
换出空 keyset，而是继续保留 last-good keyset，并把 `oidc_jwks_ready` readiness probe 标为 unhealthy。

生产应由外部 agent、Secret/ConfigMap 投影或 sidecar 负责把 IdP/Vault Transit public key 输出为 JWKS，并用原子
rename/投影更新方式轮转文件。compose demo 用 `oidc-jwks-init` 调用 `rss oidc-jwks export-vault-transit`，
从同一个 `RSS_JWT_ES256_KEY_ID` Vault Transit key 导出 ES256 public JWK，再由 server 只读挂载。

### Outbound Domain Transport mTLS

跨域同步 HTTP transport 只支持 SPIFFE/mTLS。`RSS_DOMAIN_TRANSPORT_REQUIRED_DOMAINS` 列出的每个 domain
都会在启动期构造一个 HTTPS endpoint + SPIFFE mTLS reqwest client；缺 endpoint、缺
`RSS_<DOMAIN>_DOMAIN_TRANSPORT_MTLS_SPIFFE_ALLOW_SET`、SPIRE source 不可用、当前 SVID 与
`RSS_DOMAIN_TRANSPORT_MTLS_LOCAL_SPIFFE_ID` 不一致、rustls/reqwest client 构造失败均 fail-fast。
runtime 同时注册 `domain_transport_ready` 探针，运行期若 outbound SPIFFE source 无法取得当前
X.509-SVID，或任一 resolved remote endpoint 的 TCP connect 不可达，`/readyz` 聚合为 503。该探针不反向调用
对端 `/readyz`；`bootstrap::domaintransport` 只做 topology/URL resolver；mTLS policy 在 runtime 组合根构造。

### PG TLS（`RSS_PG_SSL_MODE`）

PG 连接 TLS 默认 `VerifyFull`（强制 TLS + 校验证书链 / 主机名，零信任）。演示 postgres 未启 TLS，故
`deploy/.env.example` 显式设 `RSS_PG_SSL_MODE=disable` 降级——**不静默**，且回退方向恒为更严：缺省 / 非法值 /
空串一律 fail-closed 回 `VerifyFull`（解析见 `runtime::parse_pg_ssl_mode`）。生产删除该行（回零信任默认）
或设 `verify-full` 并经 `RSS_PG_SSL_ROOT_CERT_PATH` 注入私有 CA 根证书。`RSS_PG_SSL_ROOT_CERT_PATH` 未设置时保留
公共根行为；一旦设置，空值、不可读路径或目录路径都会在配置构造期 fail-fast，不会退回无 root cert 连接。

### Legacy ConfigValue 明文门

`config_entries.protection_scheme=0` 代表加密迁移前的 legacy plaintext `ConfigValue` 行。runtime 在
`run_migrations` 后、serving pool 建立前用 migrator 连接扫描全库；默认发现任意行即 fail-fast，避免生产在
未评估明文存量时继续对外服务。`RSS_SETTINGS_ALLOW_LEGACY_PLAINTEXT_CONFIG_VALUES=true` 只作为 backfill 前的
短期人工豁免；新写路径仍只写 encrypted scheme，serving 读路径也不再返回 scheme=0 plaintext。

存量迁移使用生产容器内 `/usr/local/bin/rss` 命令。镜像 ENTRYPOINT 仍是 `/usr/local/bin/server`，所以不能把
maintenance 参数当作普通 Docker command args 追加给默认 entrypoint；必须显式覆盖 entrypoint/command，或在运行中
容器内 exec `/usr/local/bin/rss`：

```bash
docker run --rm --entrypoint /usr/local/bin/rss <image> \
  settings-config-values maintenance --operator-service-token <token> --operator-tenant <uuid> \
  --operation both --dry-run --batch-size 500

docker exec <container> /usr/local/bin/rss \
  settings-config-values maintenance --operator-service-token <token> --operator-tenant <uuid> \
  --operation backfill --batch-size 500

# Kubernetes Job/Pod: command: ["/usr/local/bin/rss"]
# args: ["settings-config-values", "maintenance", "--operator-service-token", "<token>", "--operator-tenant", "<uuid>", "--operation", "rewrap", "--batch-size", "500"]
```

可用 `--tenant <uuid>` 分租户执行，用 `--max-rows <n>` 控制整条命令的单次处理量；`--operation both` 下该预算由
backfill 与 rewrap 共享。每次运行必须提供可验签的 `--operator-service-token <token>` 与其
`--operator-tenant <uuid>` MAC binding；命令从已验证 service principal 派生 audit subject，并写 job start/finish
durable audit。只有无 `--tenant` 的全局验证命令输出 `failed=0` 且 `remaining_plaintext=0` 后，才移除
`RSS_SETTINGS_ALLOW_LEGACY_PLAINTEXT_CONFIG_VALUES`。

Vault `min_decryption_version` 收紧不是 `remaining_plaintext=0` 的直接后续步骤：它还要求按 key rotation runbook
完成无限量 rewrap 覆盖验证，不能用带 `--max-rows` 的批次结果作为禁旧版本依据。

### S3 Object Store Canary

runtime 不再把 S3 保留为未来接线：`run()` 启动期必填 S3 env，构造 `S3RuntimeDeps`，并注册
`s3_object_store_ready` readiness probe。canary worker 启动后立即执行一轮真实对象操作，之后按
`RSS_S3_CANARY_INTERVAL_SECS` 周期重复；任一轮 put/get/delete/get-miss 失败或超时，readyz 聚合为 503。

生产 endpoint 必须是 `https://`，HTTP client 由 runtime 用 `aws-smithy-http-client` 显式选择 rustls+ring。
明文 `http://` 只给本地或 compose 演示：`RSS_S3_ALLOW_PLAINTEXT=true` 只放行 loopback；
`RSS_S3_ALLOW_PLAINTEXT=dev-container` 只放行 compose 服务名 `minio`。这两个 opt-in 都不应进入生产配置。
S3 endpoint URL 不允许 userinfo、query、fragment，避免凭据或 token 出现在 URL 与诊断面。

## 演示栈与闭环验收

```bash
make docker-smoke            # = deploy/smoke.sh；#1649 前缺 SPIRE fixture 会明确 SKIP
# 或手动：
docker compose -f deploy/docker-compose.yml up --build -d
curl -fsS http://localhost:8083/health/v1/readyz   # #1649 后应 → 200, {"overall":"healthy", ...}
curl -fsS http://localhost:8083/health/v1/healthz  # → 200（liveness 恒 200）
curl -fsS http://localhost:8083/health/v1/metrics  # → 200, content-type text/plain; version=0.0.4（Prometheus exposition）
docker compose -f deploy/docker-compose.yml down -v
```

`make docker-smoke` 为避免本机常见 8080 端口冲突，会默认导出 host 端口
`RSS_PRIMARY_HOST_PORT=18080`、`RSS_INTERNAL_HOST_PORT=18081`、`RSS_ADMIN_HOST_PORT=18082`、
`RSS_HEALTH_HOST_PORT=18083`；手动 compose 未设置这些变量时仍使用 8080-8083。

#1649 补齐 SPIRE Server/Agent、Workload API socket mount、registration entry 与 domain transport allow-set 前，
`deploy/smoke.sh` 会在 preflight 检测到缺 `SPIFFE_ENDPOINT_SOCKET` /
`RSS_DOMAIN_TRANSPORT_REQUIRED_DOMAINS` / `RSS_DOMAIN_TRANSPORT_MTLS_LOCAL_SPIFFE_ID` 时直接 SKIP，不再把
pre-SPIFFE demo 栈误判为可通过的 readyz 200 验收。

compose 会启动 `hashicorp/vault` 的 `-dev-tls` 模式，`vault-init` 初始化 `transit` mount、
`RSS_SETTINGS_CONFIG_VALUE_KEY_NAME` 对应 `derived=true` key 和 `RSS_JWT_ES256_KEY_ID` 签名 key。`oidc-jwks-init`
随后从该签名 key 的 current public key 生成 `RSS_OIDC_JWKS_PATH` 指向的 JWKS。生成的 `vault-ca.pem` 与
JWKS 分别通过只读 volume 挂给 server。生产不得使用 dev-tls；生产 JWKS 应由正式 IdP/Vault export agent 管理。

生产 runtime Vault token 至少需要允许 settings ConfigValue key 执行 encrypt/decrypt/rewrap：

```hcl
path "transit/encrypt/<settings-config-key>" { capabilities = ["update"] }
path "transit/decrypt/<settings-config-key>" { capabilities = ["update"] }
path "transit/rewrap/<settings-config-key>"  { capabilities = ["update"] }
```

创建 settings key 的 provisioning token 另需 key 管理权限，并必须使用
`vault write transit/keys/<settings-config-key> derived=true`。若 `derived=false` 或 ACL 缺失，server 启动期 self-check
会失败，运行期采样失败时 `/readyz` 返回 503 且 body 中 `keyprovider_ready` 为 unhealthy。

`deploy/smoke.sh` 在 SPIRE fixture 存在时机器断言七条闭环：

1. **readyz 就绪**：轮询 `/health/v1/readyz` 至 200（PG healthy + Redis PING healthy + Vault Transit self-check + domain transport ready + 迁移完 + 池就绪），并断言 body 含 `redis_ready` / `keyprovider_ready` / `s3_object_store_ready` / `domain_transport_ready`。
2. **healthz liveness**：`/health/v1/healthz` 恒 200。
3. **metrics 导出**：`/health/v1/metrics` 200 且 content-type `text/plain; version=0.0.4`（Prometheus exposition；`PromExporter::install` 随 `run()` 必装，端点与 `RSS_OTEL_ENDPOINT` 无关）。
4. **运行时加固**：`docker inspect` 验 `User=65532:65532` 且 `ReadonlyRootfs=true`。
5. **Vault/KeyProvider down 路径**：暂停 compose Vault 后轮询 `/readyz`，确认 `keyprovider_ready` 使响应降为 503；恢复 Vault 后回到 200。
6. **MinIO/S3 down 路径**：停止 compose MinIO 后轮询 `/readyz`，确认 `s3_object_store_ready` 使响应降为 503；恢复 MinIO 后回到 200。
7. **Redis down 路径**：停止 compose Redis 后轮询 `/readyz`，确认 `redis_ready` 使响应降为 503。

> **readyz 语义 / fail-closed**：`/readyz` 是所有探针的 **worst-of 聚合**——`configs_ready`（PG 池就绪，初值
> Down）· `redis_ready`（Redis PING，初始启动已 fail-fast，运行期 down → 503）· `domain_transport_ready`（outbound SPIFFE source 当前 SVID + resolved remote endpoint TCP reachability，启动失败 fail-fast、运行期 source/peer 不可用 → 503）· `keyprovider_ready`（Vault Transit encrypt+decrypt + wrong-AAD fail-closed self-check / sampler，启动失败 fail-fast、运行期 down → 503）· `rls_ready`（RLS 能力门）· `session_sweeper`（sessions 过期清理 worker，worker 停 → 503、sweep 失败 → Degraded）· `outbox_relay` / `event_consumer`（durable 事件 worker，#1251：worker 停
> → Unhealthy/503、relay 错误 → Degraded）。任一未就绪 → 503（fail-closed：未就绪不放流量）。裸镜像无 PG →
> 永远 503；PG/worker 就绪后转 200。`docker compose exec server sh` 不可用（distroless 无 shell，符合预期）——
> 排查用 `docker compose logs server` + `docker inspect`。

## 编排器探针（k8s）

镜像**不内置 Docker HEALTHCHECK**（distroless 无 shell，且探针应由编排器拥有）。k8s probe 指向 health
listener：

```yaml
livenessProbe:   # 进程活着即可（恒 200，不依赖 PG）
  httpGet: { path: /health/v1/healthz, port: 8083 }
  initialDelaySeconds: 5
  periodSeconds: 10
readinessProbe:  # PG/RLS + keyprovider_ready + event worker（outbox_relay/event_consumer）worst-of 就绪才收流量（未就绪 503，fail-closed）
  httpGet: { path: /health/v1/readyz, port: 8083 }
  periodSeconds: 5
securityContext:           # 与镜像 / compose 加固一致
  runAsNonRoot: true
  runAsUser: 65532
  readOnlyRootFilesystem: true
  allowPrivilegeEscalation: false
  capabilities: { drop: [ALL] }
```

## 可观测性导出（Prometheus metrics + OTLP trace，#1253）

观测导出在组合根（`assemblies/runtime`）接线，**端点位置 ≠ Prometheus 默认**，必须显式配置：

### Prometheus scrape（`metrics_path` 非默认 `/metrics`）

`/metrics` 与 healthz/readyz 同挂 Health listener（8083），完整路径 **`/health/v1/metrics`**——Prometheus 默认
`scrape_path` 是 `/metrics`，**必须显式覆盖 `metrics_path`**，否则抓 `/metrics` 得 404、被记空抓取：

```yaml
# prometheus.yml scrape_config（cluster-internal Prometheus）
scrape_configs:
  - job_name: rss-server
    metrics_path: /health/v1/metrics      # 非默认 /metrics，必须显式声明
    scheme: http
    static_configs:
      - targets: ["rss-server:8083"]       # Health listener 端口；cluster-internal 可达即可
```

k8s 经 pod annotation 等价声明（Prometheus Operator / annotation-based discovery）：

```yaml
metadata:
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/health/v1/metrics"   # 同上，非默认
    prometheus.io/port: "8083"
```

**安全边界（匿名 NoAuth 面）**：Health listener（含 `/metrics`）是 `AuthScheme::NoAuth` 匿名面——**只在
loopback / cluster-internal 暴露，绝不经公网 ingress / LoadBalancer 对外**。演示栈 8083 绑 `127.0.0.1` 落实；
生产用 `ClusterIP` Service（默认集群内可达模型）+ NetworkPolicy 限定 Prometheus 命名空间访问。**不**给 Health
listener 配公网 Ingress。

### OTLP/gRPC trace 导出（`RSS_OTEL_ENDPOINT`，按需开启）

trace 导出**默认关闭**——`RSS_OTEL_ENDPOINT` 未设时仅 fmt 结构化日志（stdout），不导出 trace、零额外依赖。设置后
按 scheme 派发 typed endpoint（误配在启动接线期 fail-fast，**不静默退回 fmt**）：

| 值 | 行为 |
|----|------|
| 未设 | 仅 fmt 日志，不导出 trace（本地开发 / 无 collector 环境默认） |
| `https://collector.example:4317` | TLS（生产默认路径，tls-ring + webpki-roots） |
| `http://localhost:4317` | 明文，**仅 loopback host** 显式 opt-in（同主机 sidecar collector） |
| `http://collector.example:4317`（非 loopback 明文） | **启动 fail-fast**（零信任：明文不出本机） |
| 非 `http(s)` scheme | **启动 fail-fast** |

OTLP endpoint/TLS/insecure 是部署契约（对齐 OpenTelemetry Collector exporter 配置范式）；导出经 batch processor
后台线程，进程优雅关停时 flush 未导出 span（`ShutdownStack` 最后 drain）。

## 非 root / 只读 rootfs / 复现性

- **非 root**：runtime 基底 `:nonroot` 内置 uid 65532；compose / k8s 再显式 `user` / `runAsUser` 审计加固。
- **只读 rootfs**：server 不写盘（日志走 stdout，rustls 无磁盘状态）；compose `read_only: true` + `tmpfs:/tmp`
  兜底，`cap_drop: [ALL]` + `no-new-privileges` 证明最小权限运行。
- **复现性**：base 镜像钉 tag（`rust:1.96.0-bookworm` = 工具链 channel；`cc-debian12:nonroot`）+ `--locked`
  锁 `Cargo.lock` + `cargo-chef` 钉版。**生产硬化**：再钉 base digest（`FROM ...@sha256:<digest>`，CI 中
  `docker buildx imagetools inspect` 解析后写回）。bit-for-bit 复现（`SOURCE_DATE_EPOCH` 等）超本切片范围，
  与 SBOM/cargo-dist 同族留作 follow-up。

## 边界（本切片不做）

- 不切 musl/scratch、不内置 health-probe 二进制、不加 k8s manifest（探针片段在上文，完整清单后续）。
- 不改全局 `[profile.release]`（体积优化用 builder 阶段 `strip` 落地，不波及全 workspace 开发构建）。
- 不接 hadolint 进 azure CI（可本地 `hadolint Dockerfile` 自检；CI 接线超本切片，待 #1145 docker-enabled agent）。

> 注：迁移序号重复（旧 `0002/0008/0009/0013`）是容器化首次 E2E 暴露的预存在 bug，**已在本 PR 同批修复**
> （重编为唯一连续 `0001–0018` + 新增 `cargo xtask migrations` 治理门，见 ADR-011 / `adapters/postgres/migrations/README.md`），
> 非本节 OOS。

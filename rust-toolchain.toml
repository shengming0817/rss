# 工具链钉选（复现优先于 "stable" 漂移）。趋近 GA 时随 MSRV 决策升版。
[toolchain]
channel = "1.96.0"
profile = "minimal"
# llvm-tools-preview 供 cargo-llvm-cov 覆盖率工具（CLAUDE.md 覆盖率门）使用。
components = ["rustfmt", "clippy", "llvm-tools-preview"]

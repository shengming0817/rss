# Rust / Cargo
/target/
# 自写 dylint lint workspace 的构建产物（嵌套 workspace，根 /target/ 锚定不覆盖）
lints/target/
**/*.rs.bk
*.pdb
# Cargo.lock 提交（本仓库产出 binary：assemblies/ 与 bins/），故不忽略。

# Coverage
*.profraw
lcov.info
coverage/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
rust-project.json

# OS
.DS_Store
Thumbs.db

# Temporary
/.claude/tools/
tmp/
*.tmp
.tmp
.azlogs
.codex-tmp/
/.local-ci-runs/
/.codex-backups/

# Local env / secrets
.env
.env.*
!.env.example
!.env.*.example
*.env.e2e
.env.e2e
.env.local
*.env.local

# Claude Code
worktrees/
.claude/scheduled_tasks.lock
.claude/agent-memory/

# Azure CLI（仓库目录下生成的运行时缓存，不入库）
.azconfig/

# 个人本地随笔（见 docs/notes/README.md，不属于项目文档）
docs/notes/

# Generated（选择性提交）
# generated/ 由 `cargo xtask` 重新生成；具体提交策略见该目录 README
bak/

# 本地镜像脚本（个人用，不入库）
hack/automation/mirror-to-github.sh

# Device command acknowledgement fact. An ACK records receipt or rejection, not convergence.
id = "identity.device-command-acked"
kind = "event"
domain = "identity"
version = "v1"
owner = "identity"
consistencyLevel = "OutboxFact"
lifecycle = "draft"
topic = "identity.device-command-acked"
delivery = "at-least-once"

[schemas]
payload = "payload.schema.json"

[capabilities.outbox]
role = "fact"

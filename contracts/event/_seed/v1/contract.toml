# Seed event 契约（draft）——payload 形态冻结 + codegen 证明，不接 subscriber。
# 格式冻结见 contracts/README.md；单一事实源 docs/rules/architecture.md §核心载体。
id = "seed.thing-happened"
kind = "event"
domain = "_seed"
version = "v1"
owner = "_framework"
consistencyLevel = "OutboxFact"
lifecycle = "draft"
# per-kind event 字段（#1035）：topic/delivery（draft 故非强制，此处演练格式 + 锁 golden）。
topic = "seed.thing-happened"
delivery = "at-least-once"

[schemas]
payload = "payload.schema.json"

[capabilities.outbox]
role = "fact"

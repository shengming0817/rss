# Audit session background projection definition retained as draft.
id = "audit.session-projection"
kind = "projection"
domain = "audit"
version = "v2"
owner = "audit"
consistencyLevel = "WorkflowEventual"
lifecycle = "draft"

[schemas]
projection = "projection.schema.json"

[capabilities.workflow]
mode = "projection"
inputs = ["identity.session-created"]
ordering = "serial-in-order"
checkpoint = "required"
replay = "required"

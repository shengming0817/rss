# 当前认证主体密码变更 HTTP 契约。CredentialRepo 单次原子 CAS 写入；并发冲突零写且 handler 不自动重试 ⇒ L1。
id = "identity.password-change"
kind = "http"
domain = "identity"
version = "v1"
owner = "identity"
consistencyLevel = "LocalTx"
lifecycle = "active"
path = "/api/v1/identity/password/change"
method = "POST"

[schemas]
request = "request.schema.json"
response = "response.schema.json"

[endpoints.http]
selfScoped = true
successStatus = 200
idempotency = "non-idempotent"

[endpoints.http.auth]
mode = "permission"
permission = "identity:profile:write"

[capabilities.localTx]
boundary = "single-domain"
txModel = "repo-atomic-cas"
retry = "bounded-transient"
commitUnknown = "not-retryable"

[effectProfile]
effects = ["auth", "write", "transaction"]

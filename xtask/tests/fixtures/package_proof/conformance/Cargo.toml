[package]
name = "rss-conformance-package-proof"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
release_package = { package = "__RELEASE_PACKAGE__", version = "=__RELEASE_VERSION__", registry = "local" }
serde_json = "1"
tokio = { version = "1", default-features = false, features = ["macros", "rt"] }

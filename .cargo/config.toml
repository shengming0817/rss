# Cargo 仓库级配置。
# alias：让文档承诺的 `cargo xtask ...` 治理入口可直接执行（否则 cargo 报 `no such command: xtask`）。
# 单一事实源：xtask/src/main.rs 子命令 + contracts/README.md。
# ref: https://doc.rust-lang.org/cargo/reference/config.html#alias
[alias]
xtask = "run -p xtask --"
